# Current Generation Flow

Date: 2026-04-24
Plan: `003-instruction-surface-generation-parity`

## Preflight

- Worktree: `/home/rookslog/workspace/projects/gsd-modifier-runtime-surfaces-20260424`
- Branch: `docs/runtime-intervention-surface-inventory`
- Head at resumed execution: `edcfd9e`
- Initial status: clean
- Materialized runtime roots in this linked worktree: `.codex/` absent, `.claude/` absent
- Prior runtime intervention inventory commits are present:
  - `a29a973` `docs(runtime-surfaces): inventory bridge intervention carriers`
  - `832b020` `docs(planning): record runtime surface inventory evidence`
  - `edcfd9e` `docs(planning): plan instruction surface parity slice`

This evidence records source and installed-package observations. It does not certify live materialized runtime output because the linked worktree has no `.codex/` or `.claude/` roots.

## Repo-Owned Initialization Producer

`tooling/portable-gsd/overlay/get-shit-done/workflows/new-project.md` is the repo-owned initialization workflow that names the generated project instruction file.

Observed source behavior:

- Runtime detection derives `RUNTIME` from execution context path or environment variables.
- The modifier overlay currently sets `INSTRUCTION_FILE="AGENTS.md"` for Codex and for every non-Codex runtime.
- The workflow later calls:

```bash
gsd-sdk query generate-claude-md --output "$INSTRUCTION_FILE"
```

- The commit and success criteria include `$INSTRUCTION_FILE`.
- Output text describes `$INSTRUCTION_FILE` as `AGENTS.md` for Codex and `AGENTS.md` otherwise.

This differs from the installed upstream workflow at `/home/rookslog/.npm/_npx/9785a834b31d581d/node_modules/get-shit-done-cc/get-shit-done/workflows/new-project.md`, which still sets `INSTRUCTION_FILE="AGENTS.md"` for Codex and `INSTRUCTION_FILE="CLAUDE.md"` otherwise, while using the same `gsd-sdk query generate-claude-md --output "$INSTRUCTION_FILE"` command.

Interpretation: the all-`AGENTS.md` filename posture is a modifier overlay decision, not the unmodified upstream default. The generator command mismatch is shared with the installed upstream workflow.

## Installed `gsd-sdk` Generator Path

`gsd-sdk` resolves to:

```text
/home/rookslog/.npm-global/bin/gsd-sdk
```

The executable points into the npx-installed package:

```text
/home/rookslog/.npm/_npx/9785a834b31d581d/node_modules/get-shit-done-cc/sdk/dist/cli.js
```

The SDK registry registers `generate-claude-md` in `sdk/dist/query/index.js` and routes it to `generateClaudeMd` from `sdk/dist/query/profile.js`.

Observed handler semantics from `sdk/src/query/profile.ts` and `sdk/dist/query/profile.js`:

- The handler signature is `generateClaudeMd(_args, projectDir)`.
- It ignores command arguments, including `--output`.
- It reads only:
  - `.planning/PROJECT.md`
  - `.planning/codebase/STACK.md`
  - `.planning/research/STACK.md`
- It returns JSON:

```json
{
  "sections": [],
  "generated": true,
  "section_count": 0
}
```

when those source files are absent.

Probe result from this worktree:

```bash
gsd-sdk query generate-claude-md --output /tmp/gsd-modifier-generated-instruction-probe.md
```

returned success and printed JSON, but did not write the requested output file.

Interpretation: the SDK query handler is not currently a file-writing instruction generator. The `new-project.md` command as written does not perform the file write it claims to perform in this worktree's installed toolchain.

## Installed CJS Generator Path

The older CJS command exists in the installed package:

```text
/home/rookslog/.npm/_npx/9785a834b31d581d/node_modules/get-shit-done-cc/get-shit-done/bin/gsd-tools.cjs
```

Although it is not listed in the short command usage banner, it accepts `generate-claude-md` and writes a file when given `--output`.

Probe command:

```bash
node /home/rookslog/.npm/_npx/9785a834b31d581d/node_modules/get-shit-done-cc/get-shit-done/bin/gsd-tools.cjs generate-claude-md --output /tmp/gsd-tools-generated-claude-probe.md
```

Probe result:

- Exit status: `0`
- Output file created
- Generated sections:
  - workflow
- Fallback sections:
  - project
  - stack
  - conventions
  - architecture
  - skills
- Profile placeholder added

The package template at `get-shit-done/templates/claude-md.md` describes this as a project-root `CLAUDE.md` template generated by `gsd-tools generate-claude-md`. It includes marker-bounded sections for project, stack, conventions, architecture, skills, workflow enforcement, and developer profile.

Interpretation: the installed package still contains a real file-writing generator, but the current workflow calls the SDK query handler instead. Switching `new-project.md` to the CJS command would be a behavior change and would also need a runtime-root-safe command path for Codex and Claude materialization.

## Related Producers And Non-Producers

`tooling/portable-gsd/overlay/get-shit-done/workflows/ingest-docs.md`:

- Detects runtime for entry continuity.
- Does not generate root instruction files.
- Explicitly keeps `$gsd-uplift-project --write` out of ingest.

`tooling/portable-gsd/overlay/get-shit-done/references/entry-runtime-uplift-continuity.md`:

- Is read-only guidance for initialization and ingest routes.
- Keeps route-state and existing uplift memory visible.
- Explicitly says entry routes must not run write-side uplift.

`tooling/codex/project_uplift.py`:

- Reads carrier specs and fingerprints carrier state.
- Writes `.planning/UPLIFT-REPORT.md`, `.planning/UPLIFT-MANIFEST.json`, and the `.planning/STATE.md` uplift section when invoked with write behavior.
- Does not generate `AGENTS.md`, `.planning/AGENTS.md`, `CLAUDE.md`, or `.planning/CLAUDE.md`.

`harness_modifier/uplift/carrier_catalog.json`:

- Tracks root and planning instruction carriers as doctrine-sensitive:
  - `.planning/AGENTS.md`
  - `.planning/CLAUDE.md`
  - `AGENTS.md`
  - `CLAUDE.md`
- Also tracks Codex runtime registry carriers:
  - `.codex/config.toml`
  - `.codex/agents/*.toml`

Interpretation: uplift reporting is a drift/proposal surface, not the instruction-file generator.

## Generated Outputs

Observed generated or materialized outputs in this worktree:

- `.codex/`: absent
- `.claude/`: absent
- `.planning/AGENTS.md`: absent
- `.planning/CLAUDE.md`: absent
- `CLAUDE.md`: absent
- `AGENTS.md`: present as the repo-maintained operator instruction file

The only probe output was written to `/tmp` and not copied into the repo.

## Open Gaps

- The modifier repo does not currently own a generator implementation for a runtime-neutral `AGENTS.md` body.
- The installed SDK handler named `generate-claude-md` does not honor `--output`.
- The installed CJS command can write a marker-bounded instruction file, but its template is explicitly documented as a `CLAUDE.md` template.
- The shared workflow source commonly uses `__PROJECT_ROOT__/.codex/get-shit-done/bin/gsd-tools.cjs` for CJS commands. Replacing the SDK call with that pattern inside a workflow mapped to Claude would require a separate runtime-root command-path decision.
- Because materialized runtime roots are absent, no live `.codex` or `.claude` generated instruction behavior was observed in this linked worktree.
