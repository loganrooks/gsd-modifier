# Runtime Intervention Surface Inventory Evidence Notes

## Preflight

- Date: 2026-04-24
- Worktree mode: clean linked worktree at `/home/rookslog/workspace/projects/gsd-modifier-runtime-surfaces-20260424`
- Branch: `docs/runtime-intervention-surface-inventory`
- Head: `99726f8`
- Initial status: clean
- Original checkout note: plan observed prior drift in `tooling/portable-gsd/overlay/config.toml`; this execution does not copy or stage that file from the original checkout.

## Delegated Report Disposition

- Agent A runtime carriers: accept. The report stays inside its assigned write scope, distinguishes source carriers from absent materialized runtime roots, and cites local paths for runtime-facing carrier claims.
- Agent B instruction generation: accept. The report distinguishes producers, consumers, read-side continuity routes, and materialization-time placeholder substitution with path-backed evidence.
- Agent C materialization contracts: accept. The report separates source validation, materialized-runtime verification, CI gates, and synthetic host-matrix proof with path-backed commands and boundaries.

## Open Questions

- The isolated worktree has no materialized `.codex/` or `.claude/` runtime roots. This inventory therefore treats live runtime-tree contents as unobserved unless a contract or setup path defines the expected materialization.
- Verification note: running the required audit refmap map command inside the isolated worktree produced the stable file/link totals but shifted local link counts from the plan baseline to `7750` existing / `84` missing because materialized runtime roots are absent there. A temp-file rerun from the original checkout, where `.codex/` and `.claude/` are present, reproduced the plan baseline of `800` markdown files, `7834` markdown links, `7787` existing links, and `47` missing links. The isolated-worktree refmap drift was not committed.
- The exact body/template source used by `new-project.md` for generated `AGENTS.md` was not identified in this slice. Later instruction-generation parity work should trace that before changing generation semantics.
- The uplift carrier catalog fingerprints `CLAUDE.md` and `.planning/CLAUDE.md`, while current Codex agent prompts explicitly reject `CLAUDE.md` as governing truth. This may be a valid cross-runtime drift-tracking distinction, but it should be named in a later parity plan.
- Claude operator-facing parity may need probes beyond the current Codex-focused runtime-specific reference scan when a change affects command wrappers, settings, hooks, or Claude-facing instruction consumption.
- Host-matrix proof remains the gate for broader host/support-language changes. It should not be implied by source-only manifest validation or by this inventory slice.
