# Checkpoint phase 04 slice 02

Timestamp: 2026-05-16T21:20:25Z
Phase: 04-first-wave-references
Slice: 02-apply-migration-of-references-verification-overrides-md
Outcome: success
Commit: (pending; this checkpoint is committed atomically with the slice)

## What was done

- Resolved the `verification-overrides.md` EOF fenced-example ambiguity through a Plan reviewer.
- Applied the reviewer-approved four-operation marker-clean migration for `get-shit-done/references/verification-overrides.md`.
- Changed the manifest entry from `mode: overwrite` to `mode: inject` for both runtimes with `parity_intent: outcome_aligned`.
- Added four inject source files under `harness_modifier/overlay/inject-sources/get-shit-done/references/verification-overrides/`.
- Deleted the old overwrite carrier at `tooling/portable-gsd/overlay/get-shit-done/references/verification-overrides.md`.
- Intentionally omitted the redundant final EOF fenced-example debt-field delta to avoid placing `GSD_MODIFIER` marker comments inside a copyable example code block.

## Files touched

- `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json` (modified)
- `tooling/portable-gsd/overlay/get-shit-done/references/verification-overrides.md` (deleted)
- `harness_modifier/overlay/inject-sources/get-shit-done/references/verification-overrides/override-format-body.md` (added)
- `harness_modifier/overlay/inject-sources/get-shit-done/references/verification-overrides/verifier-behavior-body.md` (added)
- `harness_modifier/overlay/inject-sources/get-shit-done/references/verification-overrides/creating-overrides-body.md` (added)
- `harness_modifier/overlay/inject-sources/get-shit-done/references/verification-overrides/override-lifecycle-body.md` (added)
- `.planning/initiatives/inject-migration/STATE.md` (modified)
- `.planning/initiatives/inject-migration/checkpoints/2026-05-16T212025Z-phase04-slice02.md` (added)

## Verification gates run

- `git diff --check`: PASS (exit 0)
- `python3 tooling/codex/audit_refmap.py verify .`: KNOWN BASELINE (exit 1; 6 unclassified runtime/pristine `mvp-concepts.md` links; no new unclassified links)
- `python3 harness_modifier/contract/portable_gsd_contract.py validate-manifest . --all-supported --source-only --strict`: PASS (exit 0; `hard_failures: []`)
- `python3 -m unittest discover -s tooling/codex/tests -p 'test_inject*.py'`: PASS (exit 0; 131 tests)
- `python3 -m unittest discover -s tooling/codex/tests`: KNOWN BASELINE after retry (exit 1 both times; 452 tests, 2 failures, 4 errors in stale `gsd-from-gsd2` / `gsd-plant-seed` source-inspection tests plus seed-audit and transition/uplift runtime-state helpers)
- `python3 -m json.tool tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json`: PASS (exit 0)

## Reviewer Verdict

Plan reviewer, slice ambiguity:

```text
VERDICT: PASS
REASONING: Alternative A is the safest in-scope interpretation: the current v4 catalog has no clean EOF replacement operation, `block_replace` requires a real end anchor and wraps replacement source in GSD marker comments, so full fidelity on the final fenced example would put non-example marker syntax inside a copyable `VERIFICATION.md` example. The lost two-line delta is redundant enough to drop because the marker-clean candidate preserves the debt-bearing fields in the first YAML example and preserves the normative verifier-behavior rule that any override requires `completion_mode: debt_carrying_completion` and `debt_bearing: true`. Alternative B preserves byte-level fidelity at the cost of teaching invalid marker lines inside a generated-example code block; Alternative C is disproportionate here and conflicts with Phase 4's no-new-operation-kind boundary unless the carrier truly requires that kind.
RECOMMENDATION: Proceed with Alternative A: apply the four-operation marker-clean candidate from `MIG-design-verification-overrides.md`, omit the final EOF fenced-example delta intentionally, and verify with the Slice 2 gates.
EVIDENCE:
- `.planning/initiatives/inject-migration/REVIEWERS.md:234-245` — Slice Ambiguity Resolution asks the Plan reviewer to recommend one alternative; PASS means a single recommended interpretation backed by file:line evidence.
- `.planning/initiatives/inject-migration/phases/04-first-wave-references.md:52-66` — Slice 2 is the apply slice for `verification-overrides.md`, using Slice 1 design and the declared manifest/source write set.
- `.planning/initiatives/inject-migration/phases/04-first-wave-references.md:127-132` — Phase 4 is bounded to references and explicitly does not add new operation kinds; a required new kind triggers ADR amendment.
- `.planning/initiatives/inject-migration/decisions/MIG-design-verification-overrides.md:126-129` — the proposed manifest entry is the four-operation marker-clean subset and intentionally omits the final example delta to avoid markers inside the fenced example.
- `.planning/initiatives/inject-migration/decisions/MIG-design-verification-overrides.md:221-235` — no candidate source file exists for the final EOF example; full fidelity needs either a clean EOF-capable operation or marker comments inside the fenced example.
- `tooling/portable-gsd/overlay/get-shit-done/references/verification-overrides.md:11-18` — the first YAML example already carries `completion_mode: debt_carrying_completion` and `debt_bearing: true`.
- `tooling/portable-gsd/overlay/get-shit-done/references/verification-overrides.md:120-126` — the verifier-behavior prose normatively requires debt-bearing metadata whenever overrides are applied.
- `/home/rookslog/workspace/projects/get-shit-done-upstream/get-shit-done/references/verification-overrides.md:204-227` — the upstream final fenced example ends at EOF and lacks the two debt-bearing metadata lines.
- `harness_modifier/contract/inject_operations.py:30-39` — the implemented v4 operation catalog contains seven kinds and no EOF-specific replacement kind.
- `harness_modifier/contract/inject_operations.py:107-112` — `block_replace` validation requires both `start_anchor` and `end_anchor`.
- `harness_modifier/contract/inject_operations.py:621-629` — `block_replace` finds the end anchor after the start anchor and inserts a marker-wrapped source block before that end anchor, which is why replacing inside the final fenced block would place markers inside the example.
```

gsd-debugger, full-discover gate failure:

```text
VERDICT: FAIL
REASONING: The failed gate is the already-recorded broad full-discover baseline, not a regression introduced by Phase 4 Slice 2. The four `gsd-from-gsd2` / `gsd-plant-seed` failures are stale tests asserting old `mode: overwrite` ownership and old `tooling/portable-gsd/overlay/skills/...` source paths after those carriers were intentionally reclassified to `mode: add` under `harness_modifier/overlay/`. The remaining seed-audit and transition/uplift errors execute node helpers against the materialized `.codex` runtime/state surface, which STATE already classifies as runtime-state-dependent non-pilot full-discover noise. The current slice diff is confined to `verification-overrides.md` manifest/source migration, so fixing this gate would require touching out-of-scope tests, helper/runtime state, or bootstrap gate composition.
RECOMMENDATION: Treat the failure as out of scope for Slice 2. The out-of-scope change is a separate cleanup/scope slice to update stale full-discover tests for `gsd-from-gsd2` and `gsd-plant-seed`, investigate or stabilize the seed-audit and transition/uplift runtime-state-dependent helpers, or split the broad full-regression discover out of the bootstrap/materialization gate.
EVIDENCE:
- `.planning/initiatives/inject-migration/STATE.md:42` records pre-existing full-discover baseline failures/errors as OUT OF SCOPE and names the same refreshed member class: `gsd-from-gsd2`, `gsd-plant-seed`, seed-audit helper, and transition/uplift continuity.
- `.planning/initiatives/inject-migration/STATE.md:77` records OOS #5: broad `python3 -m unittest discover -s tooling/codex/tests` fails on stale Phase-0-reclassified source-inspection tests plus runtime-state-dependent seed-audit and transition/uplift helper tests.
- `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:1007-1015` shows `skills/gsd-from-gsd2/SKILL.md` is currently `mode: add` sourced from `harness_modifier/overlay/skills/gsd-from-gsd2/SKILL.md`.
- `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:1040-1047` shows `skills/gsd-plant-seed/SKILL.md` is currently `mode: add` sourced from `harness_modifier/overlay/skills/gsd-plant-seed/SKILL.md`.
- `tooling/codex/tests/test_health_and_migration_follow_through_contract.py:14` still asserts `overlay_entry_mode("skills/gsd-from-gsd2/SKILL.md") == "overwrite"`; `tooling/codex/tests/test_health_and_migration_follow_through_contract.py:35-37` still reads the old `tooling/portable-gsd/overlay/skills/gsd-from-gsd2/SKILL.md` path.
- `tooling/codex/tests/test_seed_consumer_follow_through_contract.py:13` still asserts `overlay_entry_mode("skills/gsd-plant-seed/SKILL.md") == "overwrite"`; `tooling/codex/tests/test_seed_consumer_follow_through_contract.py:37-39` still reads the old `tooling/portable-gsd/overlay/skills/gsd-plant-seed/SKILL.md` path.
- `tooling/codex/tests/test_seed_audit_gate_follow_through_contract.py:13-15` binds audit/helper paths through the overlay and materialized `.codex/get-shit-done/bin/lib`; `tooling/codex/tests/test_seed_audit_gate_follow_through_contract.py:44-50` runs a node subprocess, matching the runtime-helper failure class.
- `tooling/codex/tests/test_transition_uplift_continuity.py:9-10` points at `.codex/get-shit-done/bin/gsd-tools.cjs`; `tooling/codex/tests/test_transition_uplift_continuity.py:97-102` runs `node ... phase complete 1 --cwd <tmpdir>`, matching the runtime-state-dependent helper class.
- `git diff --unified=0 -- tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json` output shows only the `get-shit-done/references/verification-overrides.md` entry changed from `overwrite` to `inject` with four `block_replace` operations; it does not touch the `gsd-from-gsd2` or `gsd-plant-seed` entries.
- `git diff --name-only; git ls-files --others --exclude-standard` output lists only `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json`, deleted `tooling/portable-gsd/overlay/get-shit-done/references/verification-overrides.md`, and the four new `harness_modifier/overlay/inject-sources/get-shit-done/references/verification-overrides/*.md` files.
```

Plan reviewer, full-discover gate disposition:

```text
VERDICT: PASS
REASONING: Slice 2 should not expand scope for the broad full-discover failures because the current dirty write set is confined to the pre-authorized `verification-overrides.md` carrier migration, while the failing full-discover cases are the same already-recorded non-carrier baseline surfaces: stale tests for Phase 0 reclassified `gsd-from-gsd2` and `gsd-plant-seed`, plus runtime-state-dependent seed-audit and transition/uplift helpers. The slice spec does include “existing tests still pass,” but the initiative state narrows that obligation by recording these full-discover failures as out of scope unless their shape changes or they block the carrier directly; the provided failure categories do neither. Under REVIEWERS.md semantics, this is a PASS because there is one grounded continuation decision: proceed without widening Slice 2, while logging the broad gate as known out-of-scope evidence.
RECOMMENDATION: Proceed with Slice 2 using the focused manifest/inject gates as the slice evidence, and record the broad full-discover failure as the known out-of-scope baseline; handle `gsd-from-gsd2`, `gsd-plant-seed`, seed-audit, and transition/uplift cleanup in a separate cleanup/scope slice.
EVIDENCE:
- `.planning/initiatives/inject-migration/phases/04-first-wave-references.md:52-66` declares Slice 2 as only the `verification-overrides.md` manifest/source migration, with the write set limited to deleting the old overwrite source, adding inject source files, and editing `OVERLAY-MANIFEST.json`.
- `command-output: git status --short` shows only `M tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json`, `D tooling/portable-gsd/overlay/get-shit-done/references/verification-overrides.md`, and untracked `harness_modifier/overlay/inject-sources/get-shit-done/references/verification-overrides/`.
- `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:387-460` shows the current slice changed only `get-shit-done/references/verification-overrides.md` to `mode: inject` with four `block_replace` operations for both runtimes.
- `.planning/initiatives/inject-migration/STATE.md:38-43` records the operator decision that pre-existing full-discover failures remain out of scope unless they change shape or block the pilot directly, naming `gsd-from-gsd2`, `gsd-plant-seed`, seed-audit helper, and transition/uplift continuity.
- `.planning/initiatives/inject-migration/STATE.md:66-78` records composite full-discover over-aggregation as Out-Of-Scope Surface #5 and recommends separate cleanup or gate split, not in-slice repair.
- `.planning/initiatives/inject-migration/GUARDRAILS.md:182-188` says concerns outside the inject migration must be logged, not acted on inside the loop, and surfaced later.
- `tooling/codex/tests/test_health_and_migration_follow_through_contract.py:11-14` still expects `skills/gsd-from-gsd2/SKILL.md` to be `overwrite`, while `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:1007-1015` shows it is intentionally `mode: add`.
- `tooling/codex/tests/test_seed_consumer_follow_through_contract.py:11-13` still expects `skills/gsd-plant-seed/SKILL.md` to be `overwrite`, while `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:1040-1048` shows it is intentionally `mode: add`.
- `tooling/codex/tests/test_seed_audit_gate_follow_through_contract.py:13-15` and `:25-51` run a node helper against materialized `.codex` runtime/lib state; `tooling/codex/tests/test_transition_uplift_continuity.py:9-10` and `:92-103` run `.codex/get-shit-done/bin/gsd-tools.cjs phase complete`, matching the recorded runtime-state-dependent baseline class.
- `command-output: python3 -m unittest discover -s tooling/codex/tests` failed with 452 tests, 2 failures, and 4 errors on those stale/non-carrier classes; no provided failure targets `verification-overrides.md`.
```

## Auto-Recovery

- Slice ambiguity recovery fired before edits: Plan reviewer PASS selected Alternative A.
- Verification-gate recovery fired after broad full-discover failed twice:
  - retry once: same 2 failures and 4 errors;
  - `gsd-debugger`: FAIL, root cause outside write set and already recorded baseline;
  - Plan reviewer: PASS, proceed without scope expansion and record the broad gate as known out-of-scope baseline.

## Observations

- The applied migration is not byte-for-byte identical to the prior overwrite carrier: the final `## Example VERIFICATION.md` fenced block now follows upstream and omits the two debt-bearing fields.
- The retained normative surfaces still carry the debt-bearing requirement: the first YAML example includes `completion_mode: debt_carrying_completion` / `debt_bearing: true`, and verifier behavior says overrides require those metadata fields.
- No new operation kind was added; this preserves the Phase 4 boundary.
- The broad full-discover baseline continues to impose recurring review cost and should be addressed outside this inject carrier path.

## Next expected slice

- 04-first-wave-references slice 03 — design migration of `references/agent-contracts.md`.
