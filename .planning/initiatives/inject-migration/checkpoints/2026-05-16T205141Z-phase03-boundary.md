# Checkpoint phase 03 boundary

Timestamp: 2026-05-16T20:51:41Z
Phase: 03-pilot
Slice: boundary-verification
Outcome: success
Commit: (pending; this checkpoint is committed with the boundary state update)

## What was done

- Ran the Phase 3 boundary verification stack for the `mandatory-initial-read.md` pilot.
- Confirmed direct materialization/contract gates are green for both Codex and Claude: the pilot inject entry materializes with `hard_failures: []` and `inject_failure_count: 0`.
- Triangulated the repeated `check-bootstrap.sh` exit 1: `gsd-debugger` returned ESCALATE, `adversarial-auditor-xhigh` returned PASS on the over-aggregation diagnosis, and `trajectory-verifier` returned PASS for Phase 3 boundary closure.
- Invoked the Phase 0 intent-over-literal precedent for EC3: the composite script exit remains 1, but its materialization obligation is satisfied by direct gates and the failing full-discover tests are independent of the pilot carrier.
- Added OOS #5 to STATE.md to track the recurring cost of stale/non-pilot full-discover failures before Phase 4+ boundaries normalize repeated triangulation.
- Marked Phase 3 `[x]` and advanced active work to Phase 4 Slice 1 (`references/verification-overrides.md` design).

## Files touched

- `.planning/initiatives/inject-migration/STATE.md` (modified)
- `.planning/initiatives/inject-migration/checkpoints/2026-05-16T205141Z-phase03-boundary.md` (added)
- `.planning/initiatives/inject-migration/audits/20260516-204148-phase03-boundary-bootstrap-triangulation/auditor-input.txt` (added)
- `.planning/initiatives/inject-migration/audits/20260516-204148-phase03-boundary-bootstrap-triangulation/auditor-input-output.json` (added)
- `.planning/initiatives/inject-migration/audits/20260516-204148-phase03-boundary-bootstrap-triangulation/auditor-input-stderr.log` (added)
- `.planning/initiatives/inject-migration/audits/20260516-204148-phase03-boundary-bootstrap-triangulation/audit-findings-B.md` (added)

## Verification gates run

- `bash scripts/ci/check-deterministic.sh`: PASS (exit 0; 48 tests OK; manifest contract report `hard_failures: []`)
- `bash scripts/ci/check-bootstrap.sh`: FAIL twice (exit 1; both runs materialized runtimes, then failed inside broad `python3 -m unittest discover -s tooling/codex/tests`)
- `./scripts/setup-portable-gsd-runtime.sh --runtime both`: PASS (exit 0; both runtime post-materialization reports had `hard_failures: []` and `inject_failure_count: 0`)
- `python3 harness_modifier/contract/portable_gsd_contract.py verify-materialized . --all-supported --strict`: PASS (exit 0; top-level `hard_failures: []`; both runtimes verified the `GSD_MODIFIER:references-mandatory-initial-read:extended-content` marker)
- `python3 harness_modifier/contract/harness_canary.py report . --all-supported --strict`: PASS (exit 0; summary `status: ok`, `issue_count: 0`, `parity_state: dual-runtime-aligned`)
- `rg -n "mandatory-initial-read" tooling/codex/tests/test_seed_audit_gate_follow_through_contract.py tooling/codex/tests/test_transition_uplift_continuity.py .codex/get-shit-done/bin`: PASS for independence check (exit 1 means no matches in the checked failing-test/helper surfaces)
- `git diff --check`: PASS (exit 0)
- `python3 tooling/codex/audit_refmap.py verify .`: bounded baseline (exit 1; 6 unclassified missing local targets, all in the known gitignored runtime/pristine `mvp-concepts.md` family; no new unclassified items from this boundary package)

## Reviewer Verdict

### gsd-debugger

VERDICT: ESCALATE
REASONING: The repeated `check-bootstrap.sh` failure is not a Phase 3 pilot-materialization blocker: the script materializes runtimes first, then fails at broad `python3 -m unittest discover -s tooling/codex/tests` before later contract gates can run, while the pilot carrier is `mode: inject` for both runtimes and recorded as materialized/verified with `hard_failures: []`. The observed failures line up with a pre-existing/non-pilot baseline plus gate over-aggregation: `gsd-from-gsd2` and `gsd-plant-seed` were intentionally reclassified to `mode: add`, but stale tests still expect `overwrite` and old overlay paths; the seed-audit and transition/uplift errors are runtime-state-dependent full-discover failures unrelated to `mandatory-initial-read.md`. Because Phase 3 exit criterion still literally requires bootstrap exit 0, a debugger should not waive the boundary gate inside the current write set; this is a trajectory-verifier/adversarial-auditor judgment about whether zero materialization hard failures satisfy the phase intent despite the composite script exit.
RECOMMENDATION: Invoke `trajectory-verifier` for Phase 3 boundary with this diagnosis; record the composite-gate failure in the boundary checkpoint/STATE if closure is accepted. Do not make code/test changes inside the boundary write set. If the verifier rejects closure, the smallest remediation is outside the boundary write set: either split/scope `scripts/ci/check-bootstrap.sh` so bootstrap/materialization is separable from full regression, or update the stale non-pilot full-discover tests for `gsd-from-gsd2`, `gsd-plant-seed`, seed-audit, and transition/uplift via a separate reviewer-mediated cleanup slice.
EVIDENCE:
- `scripts/ci/check-bootstrap.sh:9-16` runs runtime setup, then broad unittest discovery, then canary/manifest/materialized/refmap gates; unittest failure prevents later contract gates.
- `.planning/initiatives/inject-migration/phases/03-pilot.md:108-111` requires pilot materialization plus bootstrap exit 0 for phase closure; `.planning/initiatives/inject-migration/phases/03-pilot.md:119-125` lists the boundary commands.
- `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:322-353` shows the Phase 3 pilot carrier is `mode: inject` for both Codex and Claude with marker key `GSD_MODIFIER:references-mandatory-initial-read:extended-content`.
- `.planning/initiatives/inject-migration/STATE.md:42` records the full-discover failures as out of scope unless they change shape or block the pilot directly, and says no `mandatory-initial-read.md` test failed.
- `.planning/initiatives/inject-migration/checkpoints/2026-05-16T203147Z-phase03-slice35.md:32-35` records focused runtime-visibility tests, direct `harness_canary.py`, and `verify-materialized` all passing after the inject runtime-visibility follow-up.

Action taken: triangulated with `adversarial-auditor-xhigh` per REVIEWERS.md gate-failure table.

### adversarial-auditor-xhigh

Full findings: `.planning/initiatives/inject-migration/audits/20260516-204148-phase03-boundary-bootstrap-triangulation/audit-findings-B.md`

VERDICT: PASS
REASONING: Reviewer A's diagnosis is grounded and correctly bounded. The pilot's materialization obligation is discharged by three independent direct contract gates (`setup-portable-gsd-runtime.sh` exit 0 with `hard_failures: []` and `inject_failure_count: 0` for both runtimes; `verify-materialized --all-supported --strict` exit 0; `harness_canary.py report --all-supported --strict` exit 0 with `parity_state: dual-runtime-aligned`). The composite `check-bootstrap.sh` exit 1 is attributable to broad-discover over-aggregation: two failures (`test_health_and_migration_follow_through_contract`, `test_seed_consumer_follow_through_contract`) are directly verifiable as stale-after-Phase-0 reclassification (assertions expect `overwrite` and read paths that Phase 0 Slices 2-3 `git mv`'d out); two more are characterized in STATE.md:42 as runtime-state-dependent baseline. Reviewer A correctly identifies that waiving EC3's literal text is a trajectory-verifier/adversarial-auditor judgment, not a debugger judgment, and correctly stays in lane.
RECOMMENDATION: Concur with Reviewer A. Invoke `trajectory-verifier` for Phase 3 boundary with this triangulation result and Reviewer A's diagnosis as input. The verifier must (1) cite `test_seed_audit_gate_follow_through_contract` and `test_transition_uplift_continuity` at file:line and verify their independence from the pilot's materialized content; (2) explicitly invoke or refuse the Phase 0 boundary precedent (2026-05-16T00:22:00Z) for reading EC3 intent over literal exit code; (3) require the boundary checkpoint/STATE record both the composite-gate failure and these three caveats. If the verifier rejects closure, the smallest remediation remains outside the boundary write set: a separate reviewer-mediated cleanup slice for the four stale full-discover tests, or a `Plan`-reviewed split of `scripts/ci/check-bootstrap.sh` into materialization-only and full-regression gates. Either way, add a new STATE.md OOS entry tracking the stale full-discover test set so the recurring cost is logged.
EVIDENCE:
- `scripts/ci/check-bootstrap.sh:2,9-16` shows `set -euo pipefail` and the runtime setup -> broad unittest discovery -> contract/canary/refmap/diff gate order.
- `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:322-353` shows the pilot carrier is `mode: inject`.
- `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:948-957` and `:981-990` show `gsd-from-gsd2` and `gsd-plant-seed` are now `mode: add`.
- `tooling/codex/tests/test_health_and_migration_follow_through_contract.py:14,33-36` and `tooling/codex/tests/test_seed_consumer_follow_through_contract.py:13,38` still expect overwrite/old paths.
- `.planning/initiatives/inject-migration/checkpoints/2026-05-16T203147Z-phase03-slice35.md:33-35` records direct `harness_canary.py` and `verify-materialized` passing after the follow-up.

Action taken: forwarded both reviewer verdicts and caveats to `trajectory-verifier`.

### trajectory-verifier

VERDICT: PASS

REASONING: Phase 03 is delivered by goal intent, with one explicit boundary condition to record before Phase 4 starts. EC1 is substantively met by slice checkpoints and STATE rather than phase-plan checkbox edits: the phase plan still shows `[ ]`, but GUARDRAILS.md:211 makes phase plans operator-edited only, and Phase 0 already accepted this checkbox convention conflict. EC2 is met: the manifest is schema v4, the pilot carrier is `mode: inject` for both runtimes, the inject source exists, both materialized runtime files contain upstream text plus the marker-wrapped modifier block, and `verify-materialized` exits 0 with `hard_failures: []`. EC3 is met by the same Phase 0 intent-over-literal precedent: `check-bootstrap.sh` literally exits 1, but its materialization/direct contract surfaces are green and the failing full-discover tests are independent of `mandatory-initial-read.md`. EC4 is met conditionally: the debrief recommends proceeding with the inject design and entering Phase 4 after runtime-visibility follow-up plus boundary verification; the runtime-visibility follow-up is now complete and green.

RECOMMENDATION: Proceed with Phase 3 boundary closure, but the boundary checkpoint/STATE update must explicitly record: (1) the composite `check-bootstrap.sh` exit 1, (2) the Phase 0 precedent being invoked for EC3, and (3) a recurring-cost OOS/follow-up for the stale full-discover test set before Phase 4 begins.

EVIDENCE:
- `.planning/initiatives/inject-migration/phases/03-pilot.md:104-111` defines the four exit criteria; `:119-125` defines the boundary gates.
- `.planning/initiatives/inject-migration/GUARDRAILS.md:211` makes phase plans operator-edited only; `.planning/initiatives/inject-migration/checkpoints/2026-05-16T002240Z-phase00-boundary.md:98-104` records the prior checkbox-convention conflict.
- `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:2` is schema v4; `:322-353` defines `mandatory-initial-read.md` as `mode: inject` for both Codex and Claude.
- `.codex/get-shit-done/references/mandatory-initial-read.md:1-24` and `.claude/get-shit-done/references/mandatory-initial-read.md:1-24` contain upstream text plus the expected marker-wrapped injected block.
- `tooling/codex/tests/test_seed_audit_gate_follow_through_contract.py:13-15,25-51,53-104` uses runtime `bin/lib` audit helpers against temp seed files; no pilot reference dependency.
- `tooling/codex/tests/test_transition_uplift_continuity.py:9-10,81-90,97-103` invokes materialized `gsd-tools.cjs phase complete` against a temp `.planning` project; no pilot reference dependency.
- `.planning/initiatives/inject-migration/decisions/PILOT-DEBRIEF-mandatory-initial-read.md:77-81` recommends proceeding after follow-up and boundary verification; `:32-37` and `:68-76` record the composite-gate caveat.

Action taken: Phase 3 marked complete, OOS #5 recorded, active work advanced to Phase 4 Slice 1.

## Auto-Recovery

- `check-bootstrap.sh` failed twice at boundary, triggering verification-gate-fail auto-recovery.
- `gsd-debugger` returned ESCALATE, so the gate failure triangulated with `adversarial-auditor-xhigh`.
- `adversarial-auditor-xhigh` returned PASS on the non-pilot over-aggregation diagnosis.
- `trajectory-verifier` returned PASS for phase closure with explicit state/checkpoint caveats.

## Observations

- Phase 3 is closed on the pilot's materialization intent, not on a literal all-green `check-bootstrap.sh` exit. This is deliberate and recorded, not silent waiver.
- The recurring bootstrap/full-discover over-aggregation is now OOS #5. Phase 4 boundary will otherwise hit the same reviewer tax.
- Phase 4's first pending slice is `references/verification-overrides.md` design (`.planning/initiatives/inject-migration/phases/04-first-wave-references.md`, Slice 1).

## Next expected slice

- Phase 4 (`04-first-wave-references`) Slice 1: design migration of `references/verification-overrides.md`.
