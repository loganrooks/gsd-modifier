# Checkpoint phase 00 slice 01

Timestamp: 2026-05-15T23:40:50Z
Phase: 00-surface-cleanup
Slice: 01-reclassify-gsd-do
Outcome: success
Commit: (set post-commit — this slice's atomic commit lands the checkpoint and STATE.md together)

## What was done

- Cold-start reconciliation: confirmed `git rev-parse HEAD` = `ff9b943a111297d06d5c22811f22b123fcaa33d1` (slice 0's commit); STATE.md Last commit was lag-by-one at `35dda63...` and is reconciled in this turn's update
- Captured pre-edit `audit_refmap.py verify .` baseline: exit 0 (all items classified via `deferred_archive_gap` policy)
- Read source SKILL.md (70 lines, 2853 bytes) at `tooling/portable-gsd/overlay/skills/gsd-do/SKILL.md`
- Inspected current manifest entry for `skills/gsd-do/SKILL.md`: `parity_tier: runtime_specific`, single codex materializer with `mode: overwrite` and source pointing at `tooling/portable-gsd/overlay/...`
- Created `harness_modifier/overlay/skills/gsd-do/SKILL.md` with byte-identical content (verified via `diff <(git show HEAD:...) harness_modifier/...` → exit 0)
- Removed old source via `git rm tooling/portable-gsd/overlay/skills/gsd-do/SKILL.md`
- Edited manifest entry in `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json` (lines 909–919): `mode: overwrite → add` and `source: tooling/portable-gsd/overlay/skills/gsd-do/SKILL.md → harness_modifier/overlay/skills/gsd-do/SKILL.md`
- Updated STATE.md: slice→2, counters (carriers reclassified 0→1, slices complete 1→2), recent checkpoints row, active work narrative, Last commit reconciled to ff9b943
- Wrote this checkpoint at `checkpoints/2026-05-15T234050Z-phase00-slice01.md`

## Files touched

- `harness_modifier/overlay/skills/gsd-do/SKILL.md` (added)
- `tooling/portable-gsd/overlay/skills/gsd-do/SKILL.md` (deleted via `git rm`)
- `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json` (modified — 2 fields in 1 entry)
- `.planning/initiatives/inject-migration/STATE.md` (modified)
- `.planning/initiatives/inject-migration/checkpoints/2026-05-15T234050Z-phase00-slice01.md` (added)

## Verification gates run

- `git diff --check` → exit 0 (pass)
- `python3 tooling/codex/audit_refmap.py verify .` → exit 0 (pass; no new unclassified items introduced; Required Discipline #8 satisfied)
- Manifest entry assertion (per slice spec): `materializers.codex.mode == 'add'` AND `materializers.codex.source == 'harness_modifier/overlay/skills/gsd-do/SKILL.md'` → both true; printed "manifest entry correct"
- Content-diff verification (implicit): `diff <(git show HEAD:tooling/portable-gsd/overlay/skills/gsd-do/SKILL.md) harness_modifier/overlay/skills/gsd-do/SKILL.md` → exit 0 (byte-identical move)

## Reviewer Verdict

n/a — slice 1 spec does not mandate a slice-level reviewer (overlay carrier reclassification is pre-authorized by the slice spec per GUARDRAILS.md Reviewer-Mediated Continuation table; the pre-spec'd write set IS the authorization)

## Auto-Recovery

none — all gates passed on first execution; no retries or reviewer escalations

## Observations

- The `audit_refmap.py verify .` gate now exits 0 (not the documented exit-1 baseline). The earlier hard-stop checkpoint (`2026-05-15T225033Z-phase00-slice00.md`) recorded exit-1 with 8 unclassified items as deterministic. Either the operator-resolution commit (35dda63) or another silent fix moved those items into the policy. This is good news; the Required Discipline #8 baseline-allowance remains as defensive cover but is currently slack. STATE.md → Out-Of-Scope Surfaces #1 description may be stale and should be revisited at phase-boundary or in Phase 10 retrospective; not acting on it now per Sub-Initiative Isolation.
- The `harness_modifier/overlay/skills/gsd-do/` directory was newly created. This is NOT a forbidden top-level directory creation (Forbidden Action #9) because `harness_modifier/overlay/skills/` already existed (siblings: `gsd-propagation-review/`, `gsd-seed-migration-inventory/`, `gsd-uplift-project/`) — this is just a new sibling family member.
- The deleted `tooling/portable-gsd/overlay/skills/gsd-do/` directory was cleaned up by `git rm` once its only file was removed.
- The empty parent directory pattern (delete leaves empty dir tree) didn't apply because `git rm` removed the parent. Future slices (2, 3, 4) will follow the same shape.
- The `Last commit` lag-by-one pattern worked as documented: STATE.md showed `35dda63` (parent), `git rev-parse HEAD` showed `ff9b943` (slice 0's actual), this turn's STATE.md update reconciles to `ff9b943` (which will itself lag by one after this turn's commit).

## Next expected slice

- Phase 0 Slice 2 — Reclassify `gsd-from-gsd2` skill as modifier-owned (same shape: move `SKILL.md` from `tooling/portable-gsd/overlay/skills/gsd-from-gsd2/` to `harness_modifier/overlay/skills/gsd-from-gsd2/`; flip manifest entry `skills/gsd-from-gsd2/SKILL.md` from `mode: overwrite` to `mode: add`)
