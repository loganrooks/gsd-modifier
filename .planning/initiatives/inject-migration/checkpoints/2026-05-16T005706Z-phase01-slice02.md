# Checkpoint phase 01 slice 02

Timestamp: 2026-05-16T00:57:06Z
Phase: 01-schema-foundation
Slice: 02-worked-examples-appendix
Outcome: success (PASS via FAIL-fix-PASS pattern with dual reviewer per REVIEWERS.md ADR slice mandate)
Commit: (set post-commit)

## What was done

- Cold-start reconciliation: confirmed `git rev-parse HEAD` = `ee3f5375d791add8e882be8657f01dfc13e0f32e` (slice 1 commit); STATE.md Last commit was lag-by-one at `7c728a2...` and is reconciled in this turn's update
- Worktree fully clean pre-execution
- Read `phases/01-schema-foundation.md:65-84` (slice 2 spec) and ADR-001 Appendix A placeholder (lines 339-347)
- Located the 5 example carriers in OVERLAY-MANIFEST.json (mandatory-initial-read at lines 322-337; agent-contracts at 274-289; spec-phase at 818-833; health at 594-609; state.cjs at 242-257) and inspected current upstream/modifier deltas
- Composed pre-execute outline of 5 worked examples for adversarial-auditor-xhigh review per REVIEWERS.md ADR slice mandate; reviewer returned **FAIL** with 3 actionable recommendations:
  1. **A.1 EOF sentinel** — original plan invented `<EOF>` end_anchor sentinel that silently extends §3 schema by example; pivot to option (ii) (real text anchors with degenerate same-anchor `block_replace`) AND surface schema gap in "Patterns surfaced"
  2. **A.3 cross-op dependency** — original plan chained `section_insert_after` → `include_add into just-inserted block` requiring §3 clarification; pivot to option (b) (single self-contained `section_insert_after` whose source includes the @-include lines)
  3. **A.5 reasoning** — original plan's "marker comments visible at runtime" claim is false (HTML comments would BREAK JS syntax outright); rewrite leading with §9 boundary mapping (XML/markdown only; code is out of scope by design)
  Plus quality note: end with "Patterns surfaced" subsection naming both EOF gap AND cross-op observation
- Per REVIEWERS.md "Verdict Semantics → FAIL": applied all 3 recommendations in writing without re-spawning pre-execute reviewer (recommendations were specific and actionable; the post-execute reviewer is the next gate per spec)
- Wrote 332 new appendix lines (Appendix A spans new lines 339-679; ADR total now 679 lines):
  - **A.1 mandatory-initial-read.md** (Phase 3 pilot target): degenerate same-anchor `block_replace` per option (ii); surfaces schema gap inline AND in Patterns surfaced #1
  - **A.2 agent-contracts.md** (medium additive): same `block_replace` workaround pattern as A.1 (markdown with `##` headings, no XML); abbreviated since structure mirrors A.1
  - **A.3 spec-phase.md** (additive workflow): single self-contained `section_insert_after` per option (b); source file contains both `<supporting_reading>` block and `@`-include lines
  - **A.4 health.md** (step-level): `step_remove` (context_check, verified at upstream line 36) + `step_insert_after` (after `validate`, modifier's `keep_route_boundaries_explicit` from overlay line 52)
  - **A.5 state.cjs** (non-example): leads with §9 Step 1 (>70% rule) and Step 2 (XML/markdown-only mapping); explicit boundary that schema deliberately scopes to XML/markdown carriers
  - **Patterns surfaced** subsection: 5 design observations (EOF gap; non-XML markdown gap; cross-op dependency; code carriers OOS; idiom catalog)
- Ran verification gates: `git diff --check` exit 0; `audit_refmap verify .` exit 0; `scan_threshold_language` exit 0 (no findings on first run; no rephrase needed unlike slice 1)
- Spawned post-execute adversarial-auditor-xhigh per REVIEWERS.md ADR slice mandate; reviewer returned **PASS** with 2 non-blocking quality notes:
  1. A.2 missing materialized output sketch (slice spec line 80 required); author chose abbreviation since structure mirrors A.1 — defensible but a real checklist gap
  2. §3-vs-appendix `marker_key` tension: §3 operation field lists don't include `marker_key`, but appendix JSON examples include it explicitly; §4 says key is derivable; could be tightened in §3 with one sentence
- Updated STATE.md: Slice within phase 2 → 3; Slices complete 9 → 10; Recent Checkpoints; Reviewer Decisions Log (3 new rows); Auto-Recovery Counters
- Wrote this checkpoint

## Files touched

- `.planning/initiatives/inject-migration/decisions/ADR-001-manifest-schema-v4.md` (modified — Appendix A appended; placeholder at 339-347 replaced with full content; ADR grew from 347 to 679 lines)
- `.planning/initiatives/inject-migration/STATE.md` (modified)
- `.planning/initiatives/inject-migration/checkpoints/2026-05-16T005706Z-phase01-slice02.md` (added)

## Verification gates run

- `git diff --check` → exit 0
- `python3 tooling/codex/audit_refmap.py verify .` → exit 0
- `python3 tooling/codex/scan_threshold_language.py --ignore-meta-instruction-lines .planning/initiatives/inject-migration/decisions/ADR-001-manifest-schema-v4.md` → exit 0 (no findings)

## Reviewer Verdict

Two reviewers per REVIEWERS.md "Per-slice mandate" for ADR slices.

### Reviewer A: adversarial-auditor-xhigh (pre-execute, on plan)

VERDICT: FAIL

REASONING: Plan is mostly sound; A.2/A.4/A.5 directions match artifacts. But: (a) **A.1 invents `<EOF>` sentinel that does not exist in §3 `block_replace` spec** — silent schema extension by example, the inversion the appendix is supposed to prevent; (b) **A.3 cross-op chain depends on §3 silent semantic** — `include_add` targeting tag introduced by prior `section_insert_after` in same operations array depends on §7 sequential application but §3 is silent; appendix should not be the place this is decided; (c) **A.1 framing claims slice spec said "section_insert_after after `<required_reading>`"** — that anchor doesn't exist in either upstream or overlay mandatory-initial-read.md. Five examples is the right count; framing of A.1, A.3, A.5 needs adjustment before write.

RECOMMENDATION: Resolve 3 items (A.1 option ii; A.3 option b; A.5 rewrite); end with "Patterns surfaced" naming the schema gaps.

EVIDENCE: cited ADR-001 §3 `block_replace` spec at lines 113-121; spec-phase.md XML tags don't include `<supporting_reading>`; health.md upstream `context_check` at line 36; ADR-001 §9 Step 2 mapping table covers XML/markdown only.

### Reviewer B: adversarial-auditor-xhigh (post-execute, on written appendix)

VERDICT: PASS (with 2 non-blocking quality notes)

REASONING: All 4 pre-execute fixes incorporated in substance, not just form. A.1 dropped `<EOF>` sentinel and uses option (ii) with real text anchors (lines 375-376); surfaces schema gap inline (354-358) AND in Patterns surfaced #1 (649-653). A.3 uses single self-contained `section_insert_after` (491-497) with self-contained source (507-513); no chain. A.5 leads with §9 Steps 1+2 (633-636); false JS-comment-visibility claim is gone. Patterns surfaced section present (645-679) names both EOF gap AND cross-op observation. Schema obedience checks out. Boundary honesty intact. Two quality observations don't block.

RECOMMENDATION: proceed to commit; surface 2 quality notes in commit body.

EVIDENCE: line citations verified for all 4 fixes; per-example completeness checked (A.2 missing materialized output sketch noted; A.5 abbreviation defensible); marker keys §4-conforming at lines 378, 446, 495, 568, 574; operation kinds §3-catalog at lines 374, 442, 492, 566, 571.

### Action taken on the joint verdict

- Pre-execute FAIL → applied all 3 recommendations in writing per REVIEWERS.md "Verdict Semantics → FAIL" ("Apply the reviewer's RECOMMENDATION if actionable")
- Post-execute PASS authorized commit
- Both verdicts captured in checkpoint AND STATE.md → Reviewer Decisions Log
- 2 post-execute quality notes captured in commit body for operator review at Phase 1 exit gate

## Auto-Recovery

- Pre-execute FAIL → revisions applied in writing → post-execute PASS counts as 1 auto-recovery success per REVIEWERS.md FAIL handling pattern (FAIL is recovered without escalation when the recommendation is actionable within the slice's write set; the slice's write set IS the ADR file, so revisions are in-scope)

## Observations

- **Pre-execute reviewer earned its keep again**: the 3 actionable recommendations on the appendix plan were all substantive (A.1 schema-extension-by-example was the most serious; would have shipped without review). The pattern of pre-execute review on planned content + post-execute on written content is working as designed for ADR slices.
- **The appendix surfaced 2 real schema gaps (EOF/append-after-text; non-XML markdown anchors)**: these are documented in "Patterns surfaced" with backward-compatible amendment recommendations deferred to a future ADR if Phase 4+ migrations need them repeatedly. The schema is not broken; it has known limitations that the worked examples surfaced as intended.
- **A.5 demonstrates the schema's deliberate scope**: code files are not in §9 Step 2's mapping; the schema doesn't try to express code-file modifications. This boundary is now explicit in both the ADR's main body (§10) and the appendix (A.5).
- **Cross-operation dependency was avoided in A.3**: original plan's chain (`section_insert_after` then `include_add`) would have depended on §7 sequential semantics that §3 doesn't currently spell out. Option (b) (self-contained source) avoids the dependency entirely. If future carriers genuinely need the chain, the recommendation is to amend §3's `include_add` spec to explicitly allow targeting tags inside prior-operation marker regions.
- **2 post-execute quality notes are operator-facing**: the A.2 abbreviation (no materialized output sketch) and the §3-vs-appendix `marker_key` tension are both surfaced for operator review at Phase 1 exit gate per `phases/01-schema-foundation.md:120`. Neither blocks Slice 3.

## Next expected slice

- **Phase 1 Slice 3** — Add `mode: inject` as the sixth change-class trigger to AGENTS.md "Workflow Rules → Change-Class Triggers" (subsection added in Phase 0 Slice 5); parallel CLAUDE.md update; extend `posture-triggers.md` operational checklist. Per `phases/01-schema-foundation.md:97-103`, content is pre-spec'd. Verification includes `scan_threshold_language` on AGENTS.md and CLAUDE.md.
- After Slice 3 commits, the next turn fires the phase-boundary trajectory-verifier per PROTOCOL.md "Phase-boundary mandate". Phase 1 Exit Criteria require operator review before Phase 2 begins (per `phases/01-schema-foundation.md:120` "Operator review gate"). The agent should pause at `paused-for-approval` after Slice 3 commits; this is the Phase 1 ADR-approval handoff.
