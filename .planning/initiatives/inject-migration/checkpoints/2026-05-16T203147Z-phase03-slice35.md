# Checkpoint phase 03 slice 3.5

Timestamp: 2026-05-16T20:31:47Z
Phase: 03-pilot
Slice: 3.5-runtime-visibility-follow-up
Outcome: success
Commit: (committed with this slice)

## What was done

- Reconciled the expected lag-by-one state: prior STATE Last commit was `f64db5ae2182918ad86c24c642a7c8fcae409b37`; actual HEAD was `0c0e0c17343c5dac728bc95dc0b046b3c60e61ce`, the immediately following Slice 3 commit.
- Ran Plan review for the reviewer-mediated follow-up scope.
- Updated `runtime_visibility.py` so `mode: inject` entries are treated as manifest-backed live-target operation checks instead of source-backed overlay text checks.
- Reused `inject_operations.verify_inject_state` for runtime-visibility inject classification.
- Added focused runtime-visibility tests for verified and missing-marker inject states.
- Ran the required Claude-backed adversarial contract review before commit.
- Cleared the direct `harness_canary.py report . --all-supported --strict` blocker.

## Files touched

- `harness_modifier/contract/runtime_visibility.py` (modified)
- `tooling/codex/tests/test_runtime_visibility.py` (modified)
- `.planning/initiatives/inject-migration/audits/20260516-202646-phase03-slice35-runtime-visibility-contract/auditor-input.txt` (added)
- `.planning/initiatives/inject-migration/audits/20260516-202646-phase03-slice35-runtime-visibility-contract/auditor-output.json` (added)
- `.planning/initiatives/inject-migration/audits/20260516-202646-phase03-slice35-runtime-visibility-contract/auditor-stderr.log` (added)
- `.planning/initiatives/inject-migration/audits/20260516-202646-phase03-slice35-runtime-visibility-contract/audit-findings.md` (added)
- `.planning/initiatives/inject-migration/STATE.md` (modified)
- `.planning/initiatives/inject-migration/checkpoints/2026-05-16T203147Z-phase03-slice35.md` (added)

## Verification gates run

- `python3 -m py_compile harness_modifier/contract/runtime_visibility.py tooling/codex/tests/test_runtime_visibility.py`: pass, exit 0
- `python3 -m unittest tooling.codex.tests.test_runtime_visibility`: pass, 8 tests OK
- `python3 harness_modifier/contract/harness_canary.py report . --all-supported --strict`: pass, exit 0; `parity_state: dual-runtime-aligned`, `unknown_live_drift: 0`, `issue_count: 0`
- `python3 harness_modifier/contract/portable_gsd_contract.py verify-materialized . --all-supported --strict`: pass, exit 0; top-level `hard_failures: []`, both runtimes `inject_failure_count: 0`
- `git diff --check`: pass, exit 0
- `python3 tooling/codex/audit_refmap.py verify .`: expected baseline exit 1; 6 unclassified runtime-mirror links in `.codex/.claude`; no new Markdown-link drift from this slice

## Reviewer Verdict

### Plan reviewer

VERDICT: PASS

REASONING: Recommend Alternative A. The safe expansion is narrow because the failure is localized to runtime visibility's assumption that every manifest-backed surface has a readable source file: `portable_gsd_contract.py` normalizes inject materializers with `source_path: ""`, while `runtime_visibility.py` unconditionally converts that to a path and reads it, causing the observed `IsADirectoryError`. `harness_canary.py` is only a consumer of `rv.build_report(...)` and has no demonstrated independent call-contract defect, so editing it now would widen scope on theory rather than evidence. This is a legitimate Phase 3 follow-up because Slice 3 proved pilot materialization, the debrief and STATE both record runtime visibility as the remaining phase-boundary blocker, and the phase-boundary plan requires `harness_canary.py report` before Phase 3 can close. Contract-file edits still require the separate adversarial contract review path, but that does not block choosing the write set.

RECOMMENDATION: Proceed with Alternative A only. Allowed write set: `harness_modifier/contract/runtime_visibility.py`, `tooling/codex/tests/test_runtime_visibility.py`, `.planning/initiatives/inject-migration/STATE.md`, and one new `.planning/initiatives/inject-migration/checkpoints/<UTC>-phase03-slice35.md` checkpoint. Do not edit `harness_modifier/contract/harness_canary.py` unless the focused runtime-visibility tests or the direct canary rerun proves its call contract needs a change. Verification gates: `python3 -m py_compile harness_modifier/contract/runtime_visibility.py tooling/codex/tests/test_runtime_visibility.py`; `python3 -m unittest tooling.codex.tests.test_runtime_visibility`; `python3 harness_modifier/contract/harness_canary.py report . --all-supported --strict`; `python3 harness_modifier/contract/portable_gsd_contract.py verify-materialized . --all-supported --strict`; `git diff --check`; and `python3 tooling/codex/audit_refmap.py verify .` with any pre-existing baseline distinction recorded. Before commit, run the required adversarial contract review for the `harness_modifier/contract/` edit.

EVIDENCE: Plan reviewer cited `phases/03-pilot.md` objective/slice/boundary lines, `GUARDRAILS.md` write-set and reviewer-mediation rules, STATE blocker text, pilot debrief canary failure text, `portable_gsd_contract.py` inject normalization and materialized verification code, `runtime_visibility.py` source-path read site, `harness_canary.py` consumer call site, the manifest's pilot inject entry, and the reproduced `IsADirectoryError`.

### adversarial-auditor-xhigh

Audit artifacts:

- `.planning/initiatives/inject-migration/audits/20260516-202646-phase03-slice35-runtime-visibility-contract/auditor-input.txt`
- `.planning/initiatives/inject-migration/audits/20260516-202646-phase03-slice35-runtime-visibility-contract/auditor-output.json`
- `.planning/initiatives/inject-migration/audits/20260516-202646-phase03-slice35-runtime-visibility-contract/auditor-stderr.log`
- `.planning/initiatives/inject-migration/audits/20260516-202646-phase03-slice35-runtime-visibility-contract/audit-findings.md`

Verdict metadata from JSON output: `is_error: false`, `duration_ms: 200901`, `num_turns: 20`, `total_cost_usd: 1.3304624999999999`.

VERDICT: PASS

REASONING: The diff is the smallest correct fix for the documented blocker. `runtime_visibility.py` previously assumed every manifest-backed surface had a readable `source_path`, but `portable_gsd_contract.normalize_overlay_inject_materializer_entry` normalizes inject specs to `source_path: ""`, which resolved to `Path(".")` and crashed the canary report. The patch gates `source_path` reads behind `not is_inject and overlay_path is not None` while treating inject entries as overlay-covered, then reuses the same `inject_operations.verify_inject_state` engine that `portable_gsd_contract.build_materialization_report_for_roots` already calls for `verify-materialized`. Verified-inject is mapped to `INTENTIONAL`/`SUB_INJECT_VERIFIED`; failed-inject is mapped to `UNKNOWN`/`SUB_INJECT_UNVERIFIED`, which `build_parity_assessment` already promotes into `dual-runtime-conflict`, which in turn flips `check_dual_runtime_core_alignment` in `harness_canary.py` to `issue`. New per-entry fields (`mode`, `inject_verification`) are purely additive; `harness_canary.py` only consumes the top-level `summary` and `parity_details`, so no canary edit is needed. The existing pre-flight verification corroborates the surface change.

RECOMMENDATION: proceed

EVIDENCE: See `audit-findings.md` for line-cited evidence. Key cited surfaces were `runtime_visibility.py` inject gate/source-read guard/inject branch/entry shape, `portable_gsd_contract.py` inject normalization and materialized verification code, `inject_operations.py` verify engine, `test_runtime_visibility.py` regression tests, `STATE.md` blocker, `GUARDRAILS.md` contract-review mandate, and `phases/03-pilot.md` boundary gate.

## Auto-Recovery

- Reviewer-mediated continuation: Plan PASS selected the narrow follow-up write set.
- Contract-surface reviewer: adversarial-auditor-xhigh PASS authorized committing the `harness_modifier/contract/` edit.

## Observations

- `harness_canary.py` did not need an edit. Its direct strict run passes after `runtime_visibility.py` handles inject entries.
- Failed inject verification now contributes to `unknown_live_drift`, which means a real marker/state problem remains strict-failing rather than being hidden as selective boundary noise.
- `audit_refmap.py verify .` remains at the known 6 unclassified runtime-mirror baseline after this slice.

## Next expected slice

- Phase 3 boundary verification. Run the phase-boundary verifier before marking Phase 3 complete or moving to Phase 4.
