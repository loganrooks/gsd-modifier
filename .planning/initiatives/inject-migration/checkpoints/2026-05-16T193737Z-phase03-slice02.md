# Checkpoint phase 03 slice 02

Timestamp: 2026-05-16T19:37:37Z
Phase: 03-pilot
Slice: 02-apply-the-pilot-migration
Outcome: success
Commit: (pending at checkpoint write; parent HEAD `63fea17c6ba0dd68fefc73126dc4ecbe94b516ea`)

## What was done

- Reconciled `STATE.md`'s expected lag-by-one commit pointer from Phase 3 Slice 1 to current HEAD.
- Bumped `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json` `schema_version` from `3` to `4`.
- Changed the `get-shit-done/references/mandatory-initial-read.md` codex and claude materializers from `mode: overwrite` to `mode: inject`.
- Added one `block_replace` operation per runtime using the Slice 1 newline-preserving same-anchor design and marker key `GSD_MODIFIER:references-mandatory-initial-read:extended-content`.
- Created `harness_modifier/overlay/inject-sources/get-shit-done/references/mandatory-initial-read/extended-content.md` with the modifier-owned reading packet tiers and contextual reread rules.
- Deleted the old overwrite carrier at `tooling/portable-gsd/overlay/get-shit-done/references/mandatory-initial-read.md`.
- Auto-recovered stale source-inspection tests that still assumed the deleted overwrite source existed, after `gsd-debugger` identified the issue and `Plan` authorized a narrow test/helper scope expansion.

## Files touched

- `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json` (modified)
- `harness_modifier/overlay/inject-sources/get-shit-done/references/mandatory-initial-read/extended-content.md` (added)
- `tooling/portable-gsd/overlay/get-shit-done/references/mandatory-initial-read.md` (deleted)
- `tooling/codex/tests/overlay_paths.py` (modified by Plan-authorized scope expansion)
- `tooling/codex/tests/test_read_packet_tiers_contract.py` (modified by Plan-authorized scope expansion)
- `tooling/codex/tests/test_entry_runtime_continuity_shared_reference_contract.py` (modified by Plan-authorized scope expansion)
- `.planning/initiatives/inject-migration/STATE.md` (modified)
- `.planning/initiatives/inject-migration/checkpoints/2026-05-16T193737Z-phase03-slice02.md` (added)

## Verification gates run

- `git diff --check`: pass (exit 0)
- `python3 tooling/codex/audit_refmap.py verify .`: bounded baseline failure (exit 1; 7 unclassified missing local targets, matching known OOS #1 shape; no new unclassified item from this slice)
- `python3 harness_modifier/contract/portable_gsd_contract.py validate-manifest . --all-supported --source-only --strict`: pass (exit 0; `hard_failures: []`; manifest schema version 4 observed for both runtimes)
- `python3 -m unittest tooling.codex.tests.test_read_packet_tiers_contract tooling.codex.tests.test_entry_runtime_continuity_shared_reference_contract`: pass (9 tests, exit 0)
- `python3 -m py_compile tooling/codex/tests/overlay_paths.py tooling/codex/tests/test_read_packet_tiers_contract.py tooling/codex/tests/test_entry_runtime_continuity_shared_reference_contract.py`: pass (exit 0)
- `python3 -m unittest discover -s tooling/codex/tests -p 'test_inject*.py'`: pass (131 tests, exit 0)
- `python3 -m unittest tooling.codex.tests.test_inject_apply tooling.codex.tests.test_inject_back_compat tooling.codex.tests.test_inject_markers tooling.codex.tests.test_inject_operations_thorough tooling.codex.tests.test_inject_schema tooling.codex.tests.test_inject_verify`: pass (131 tests, exit 0)
- `python3 -m unittest discover -s tooling/codex/tests`: bounded baseline failure after auto-recovery (exit 1; 450 tests run; 3 failures + 3 errors on the same unrelated stale surfaces: `gsd-from-gsd2`, `gsd-plant-seed`, seed-audit helper, and state-snapshot future carry; no `mandatory-initial-read.md` failures remain)

## Reviewer Verdict

### `gsd-debugger` gate-failure recovery

VERDICT: FAIL
REASONING: The failed gate is deterministic and is not evidence that the pilot manifest edit is malformed; it is evidence that the unittest contract surface was not migrated with the carrier. The applied manifest now declares `schema_version: 4` and `mode: inject` for `get-shit-done/references/mandatory-initial-read.md`, with both runtimes reading the new inject source, which matches the slice purpose. The failing tests still assert the old overwrite contract and hardcode the deleted overwrite source path, so the slice exposed stale test/helper assumptions. The correct fix requires editing `tooling/codex/tests/...` and likely `tooling/codex/tests/overlay_paths.py` or local manifest-reading logic, which is outside the declared slice write set.
RECOMMENDATION: Expand Plan reviewer scope before continuing. Update the affected tests to treat `mandatory-initial-read.md` as an inject carrier: assert `overlay_entry_mode(...) == "inject"`, read the operation source `harness_modifier/overlay/inject-sources/get-shit-done/references/mandatory-initial-read/extended-content.md` or a new helper that resolves inject operation sources, and keep the existing content assertions against that inject source or against materialized runtime output as appropriate. Estimated effort: small, roughly 30-60 minutes, plus rerunning the focused tests and full unittest gate.
EVIDENCE:
- `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:322` defines the migrated entry for `get-shit-done/references/mandatory-initial-read.md`; lines 327-349 show both `codex` and `claude` materializers now use `mode: inject` with `extended-content.md`.
- `harness_modifier/overlay/inject-sources/get-shit-done/references/mandatory-initial-read/extended-content.md:1` contains the new reading-packet content expected by the migrated carrier.
- `tooling/codex/tests/test_read_packet_tiers_contract.py:11` still asserts the carrier mode is `"overwrite"` and lines 18-21 still read the deleted overwrite file directly.
- `tooling/codex/tests/test_entry_runtime_continuity_shared_reference_contract.py:60` still reads `tooling/portable-gsd/overlay/get-shit-done/references/mandatory-initial-read.md`, which the slice intentionally deleted.
- `tooling/codex/tests/overlay_paths.py:43` resolves only direct `source` fields and does not model `mode: inject` operation sources, so shared test helpers are not yet schema-v4-aware for source inspection.
- `harness_modifier/contract/portable_gsd_contract.py:366` explicitly supports `mode: inject` for schema version 4, and lines 569-573 exclude inject entries from missing source-file checks; `validate-manifest . --all-supported --source-only --strict` passes with zero hard failures.

### `Plan` scope-expansion review

VERDICT: PASS
REASONING: Alternative 2 is the correct resolution: the slice already pre-authorizes migrating the pilot carrier from overwrite to inject, and the deterministic failures show stale test/helper assumptions about that same carrier rather than a malformed manifest or runtime contract failure. Expanding only the source-inspection test surface keeps the work aligned with Slice 2 because it updates verification to understand the schema-v4 carrier it is already required to validate, without widening into unrelated baseline failures or broader Phase 0 cleanup.
RECOMMENDATION: Expand the Slice 2 write set narrowly to include `tooling/codex/tests/overlay_paths.py`, `tooling/codex/tests/test_read_packet_tiers_contract.py`, and `tooling/codex/tests/test_entry_runtime_continuity_shared_reference_contract.py`. The allowed change is limited to making these tests/helper paths schema-v4-aware for `get-shit-done/references/mandatory-initial-read.md`: assert `mode: inject`, resolve the relevant inject operation source such as `harness_modifier/overlay/inject-sources/get-shit-done/references/mandatory-initial-read/extended-content.md`, and preserve existing content/continuity assertions against either that inject source or materialized runtime output. Do not include the known baseline failures around `gsd-from-gsd2`, `gsd-plant-seed`, seed audit helper, or `test_state_snapshot_future_carry` in this slice.
EVIDENCE:
- The declared Slice 2 purpose is to migrate only `get-shit-done/references/mandatory-initial-read.md` from `mode: overwrite` to `mode: inject` while bumping `schema_version` from 3 to 4.
- The failing tests still assert the old overwrite contract and read the deleted overwrite source path, so they are stale verification surfaces for the migrated pilot carrier.
- `portable_gsd_contract.py` already supports `mode: inject` under schema version 4, and `validate-manifest . --all-supported --source-only --strict` passes, so the manifest/schema path is not the failing surface.
- The proposed additional write set is confined to test/helper files that inspect the migrated carrier and does not alter runtime overlay behavior, manifest semantics, or unrelated baseline failures.

Action taken: accepted the Plan PASS. Added `overlay_inject_source_paths(...)` to the test helper and updated only the two stale `mandatory-initial-read.md` test modules.

## Auto-Recovery

- Verification gate failure recovery fired for `python3 -m unittest discover -s tooling/codex/tests`.
- Retried the gate once; failure reproduced deterministically.
- Spawned `gsd-debugger`, which returned FAIL because the fix required a test/helper scope expansion.
- Spawned `Plan`, which returned PASS for a narrow expansion limited to the migrated carrier's source-inspection tests/helper.
- Applied the Plan-authorized changes and reran the affected tests plus the full gate. The migrated carrier failures cleared; only unrelated baseline stale surfaces remain.

## Observations

- The phase plan's literal shorthand `python3 -m unittest tooling.codex.tests.test_inject_*` is not a valid unittest module pattern in this shell/runtime; it fails with `ModuleNotFoundError: No module named 'tooling.codex.tests.test_inject_*'`. The Slice 1 design's discover-form gate and the explicit six-module invocation both pass 131/131 focused inject tests.
- Slice 3 remains exposed to the known installer/bootstrap blocker recorded in `STATE.md` Out-Of-Scope Surface #3.
- Slice 3 is also where the Phase 2 TODO around `harness_modifier/compatibility/declaration.json` `overlay_schema_version` is likely to become empirical, because `verify-materialized` checks compatibility declaration schema version against the live overlay manifest.

## Next expected slice

- 03-pilot slice 03 — post-migration verification and pilot debrief.
