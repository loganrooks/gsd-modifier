# Checkpoint phase 04 slice 04

Timestamp: 2026-05-16T21:42:25Z
Phase: 04-first-wave-references
Slice: 04-apply-migration-of-references-agent-contracts-md
Outcome: success
Commit: (pending; this checkpoint is committed atomically with the slice)

## What was done

- Converted `get-shit-done/references/agent-contracts.md` from `mode: overwrite` to dual-runtime `mode: inject`.
- Added `parity_intent: outcome_aligned` to the manifest entry.
- Added three `block_replace` operations for marker rules, handoff contracts, and the clean/debt completion note.
- Created three modifier-owned source fragments under `harness_modifier/overlay/inject-sources/get-shit-done/references/agent-contracts/`.
- Deleted the old full-file overwrite carrier from `tooling/portable-gsd/overlay/get-shit-done/references/agent-contracts.md`.
- Ran focused carrier gates and dispositioned the recurring broad full-discover baseline via reviewer recovery.

## Files touched

- `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json` (modified)
- `tooling/portable-gsd/overlay/get-shit-done/references/agent-contracts.md` (deleted)
- `harness_modifier/overlay/inject-sources/get-shit-done/references/agent-contracts/marker-rules-body.md` (added)
- `harness_modifier/overlay/inject-sources/get-shit-done/references/agent-contracts/handoff-contracts-body.md` (added)
- `harness_modifier/overlay/inject-sources/get-shit-done/references/agent-contracts/clean-completion-note.md` (added)
- `.planning/initiatives/inject-migration/STATE.md` (modified)
- `.planning/initiatives/inject-migration/checkpoints/2026-05-16T214225Z-phase04-slice04.md` (added)

## Verification gates run

- Pure-function operation check: PASS (`apply_inject_operations` applied 3 operations; `verify_inject_state` returned `passed=True` with all 3 operations `verified`)
- `git diff --check`: PASS (exit 0)
- `python3 harness_modifier/contract/portable_gsd_contract.py validate-manifest . --all-supported --source-only --strict`: PASS (exit 0; `hard_failures: []`)
- `python3 -m unittest discover -s tooling/codex/tests -p 'test_inject*.py'`: PASS (exit 0; 131 tests)
- `python3 tooling/codex/audit_refmap.py verify .`: KNOWN BASELINE (exit 1; 6 unclassified runtime/pristine `mvp-concepts.md` links; no new unclassified links)
- `python3 -m unittest discover -s tooling/codex/tests`: KNOWN BASELINE (exit 1; 452 tests; 2 failures and 4 errors in stale wrapper/source-inspection and runtime-state helper tests, none naming `agent-contracts.md`)
- `python3 -m unittest discover -s tooling/codex/tests` retry: KNOWN BASELINE (exit 1; same 2 failures and 4 errors)

## Reviewer Verdict

### gsd-debugger

VERDICT: FAIL
REASONING: The failure is deterministic but not caused by the Phase 4 Slice 4 `agent-contracts.md` inject migration. A clean detached `HEAD` worktree, before the Slice 4 edits, already fails the same broad unittest gate family. The direct wrapper failures come from stale or drifted broad-gate expectations: tests expect `overwrite` and read `tooling/portable-gsd/overlay/skills/.../SKILL.md`, while the manifest already declares those skill wrappers as `add` sourced from `harness_modifier/overlay/skills/...`. Fixing this would require changing tests/manifest/source surfaces outside the Slice 4 write set.
RECOMMENDATION: Outside scope; do not self-recover inside Phase 4 Slice 4. Route a separate broad-gate repair/disposition for the pre-existing wrapper contract drift and unrelated subprocess test failures.
EVIDENCE:
- `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:1052` and `:1057` show `skills/gsd-from-gsd2/SKILL.md` as `mode: "add"`, source `harness_modifier/overlay/skills/gsd-from-gsd2/SKILL.md`.
- `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:1085` and `:1090` show `skills/gsd-plant-seed/SKILL.md` as `mode: "add"`, source `harness_modifier/overlay/skills/gsd-plant-seed/SKILL.md`.
- `tooling/codex/tests/test_health_and_migration_follow_through_contract.py:14` expects `overwrite`; `:36-37` reads the absent `tooling/portable-gsd/overlay/skills/gsd-from-gsd2/SKILL.md`.
- `tooling/codex/tests/test_seed_consumer_follow_through_contract.py:13` expects `overwrite`; `:38-39` reads the absent `tooling/portable-gsd/overlay/skills/gsd-plant-seed/SKILL.md`.
- Clean detached `HEAD` baseline at `47776d2` ran `python3 -m unittest discover -s tooling/codex/tests`: `Ran 452 tests ... FAILED (failures=2, errors=5)`, including the same wrapper assertion/FileNotFound errors before the Slice 4 diff.

### Plan

VERDICT: PASS
REASONING: Phase 4 Slice 4 should not expand scope because the passed focused gates cover the agent-contracts carrier migration, while the broad unittest failures are deterministic pre-existing drift in unrelated wrapper expectations/helpers. The failing tests name `skills/gsd-from-gsd2` and `skills/gsd-plant-seed`, not `get-shit-done/references/agent-contracts.md`, the manifest entry under migration, or the new agent-contracts inject source path; the debugger also established the same broad-gate family already failed at clean detached baseline `47776d2` before this slice.
RECOMMENDATION: Proceed with the current Slice 4 commit without repairing the broad-suite tests/helpers in this slice; record the broad unittest result as a known out-of-scope baseline gate failure and route a separate broad-gate repair/disposition for the stale wrapper contract drift and subprocess helper failures.
EVIDENCE:
- `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:274-329` shows the Slice 4 target is `get-shit-done/references/agent-contracts.md`, migrated to `mode: "inject"` with sources under `harness_modifier/overlay/inject-sources/get-shit-done/references/agent-contracts/`.
- `python3 harness_modifier/contract/portable_gsd_contract.py validate-manifest . --all-supported --source-only --strict`: exit 0, `hard_failures []`.
- `python3 -m unittest discover -s tooling/codex/tests -p 'test_inject*.py'`: 131/131 pass.
- `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:1052-1059` declares `skills/gsd-from-gsd2/SKILL.md` as `mode: "add"` from `harness_modifier/overlay/skills/gsd-from-gsd2/SKILL.md`, while `tooling/codex/tests/test_health_and_migration_follow_through_contract.py:14` expects `overwrite` and `:36-37` reads the old `tooling/portable-gsd/overlay/skills/gsd-from-gsd2/SKILL.md` path.
- `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:1085-1092` declares `skills/gsd-plant-seed/SKILL.md` as `mode: "add"` from `harness_modifier/overlay/skills/gsd-plant-seed/SKILL.md`, while `tooling/codex/tests/test_seed_consumer_follow_through_contract.py:13` expects `overwrite` and `:38-39` reads the old `tooling/portable-gsd/overlay/skills/gsd-plant-seed/SKILL.md` path.
- Clean detached `HEAD` baseline at `47776d2` already failed `python3 -m unittest discover -s tooling/codex/tests` with the same wrapper assertion/FileNotFound failure family before the Slice 4 diff.

## Auto-Recovery

- Broad full-discover failed, was retried once, and failed with the same 2 failures / 4 errors.
- `gsd-debugger` returned FAIL because the root cause is outside the Slice 4 write set.
- Plan reviewer returned PASS to proceed without scope expansion, recording the broad gate as known out-of-scope baseline.

## Observations

- The focused carrier path is clean: source-only manifest validation passes, inject unit tests pass, and a pure-function apply/verify check verifies all three operations.
- The broad unittest failure remains the same class recorded in OOS #5: stale expectations around Phase 0 reclassified wrapper skills plus runtime-state helper failures.
- No failed broad-suite test names `agent-contracts.md`, the new inject source directory, or the manifest entry under migration.

## Next expected slice

- 04-first-wave-references slice 05 - design migration of `references/planner-reviews.md`.
