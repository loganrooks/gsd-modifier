# Checkpoint phase 04 slice 08

Timestamp: 2026-05-16T22:31:36Z
Phase: 04-first-wave-references
Slice: 08-apply-migration-of-references-planning-config-md
Outcome: success
Commit: (pending pre-commit; this checkpoint is committed with the slice)

## What was done

- Resolved the Slice 8 ambiguity through a Plan reviewer before editing.
- Applied the Plan-approved source-freshening migration for `get-shit-done/references/planning-config.md`.
- Converted the manifest entry from dual-runtime `mode: overwrite` to dual-runtime `mode: inject`.
- Added seven `block_replace` source bodies for the modifier-owned command, manager-flag, and `workflow.discuss_mode` deltas.
- Deleted the old overwrite carrier.
- Intentionally let newer upstream field documentation and the upstream `mode` row flow through instead of preserving stale overwrite omissions.

## Files touched

- `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json` (modified)
- `harness_modifier/overlay/inject-sources/get-shit-done/references/planning-config/commit-docs-gsd-tools-body.md` (added)
- `harness_modifier/overlay/inject-sources/get-shit-done/references/planning-config/commit-via-cli-body.md` (added)
- `harness_modifier/overlay/inject-sources/get-shit-done/references/planning-config/init-execute-phase-command.md` (added)
- `harness_modifier/overlay/inject-sources/get-shit-done/references/planning-config/state-load-command.md` (added)
- `harness_modifier/overlay/inject-sources/get-shit-done/references/planning-config/top-manager-discuss-row.md` (added)
- `harness_modifier/overlay/inject-sources/get-shit-done/references/planning-config/manager-fields-discuss-row.md` (added)
- `harness_modifier/overlay/inject-sources/get-shit-done/references/planning-config/team-example-discuss-mode.md` (added)
- `tooling/portable-gsd/overlay/get-shit-done/references/planning-config.md` (deleted)
- `.planning/initiatives/inject-migration/STATE.md` (modified)
- `.planning/initiatives/inject-migration/checkpoints/2026-05-16T223136Z-phase04-slice08.md` (added)

## Verification gates run

- `python3 -c 'import json; json.load(open("tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json")); print("valid")'`: pass, exit 0.
- `git diff --check`: pass, exit 0.
- `python3 tooling/codex/audit_refmap.py verify .`: expected baseline fail, exit 1. Reported 6 unclassified missing local targets, all runtime/pristine `mvp-concepts.md` links already documented as the current known baseline; no new `planning-config.md` item.
- `python3 harness_modifier/contract/portable_gsd_contract.py validate-manifest . --all-supported --source-only --strict`: pass, exit 0, `hard_failures: []`.
- `python3 -m unittest discover -s tooling/codex/tests -p 'test_inject*.py'`: pass, 131 tests OK.
- Pure-function apply/verify check against real upstream `planning-config.md` and the new manifest operations: pass, seven `block_replace` records applied, `verify_passed=True`, all seven operation statuses `verified`, materialized content contains `gsd-tools.cjs`, upstream security and ship fields, no stale `code-first` mode row, and the upstream `interactive` / `yolo` mode row.
- `python3 -m unittest discover -s tooling/codex/tests`: expected baseline fail, repeated twice. Both runs reported 452 tests with 2 failures and 4 errors in stale `gsd-from-gsd2` / `gsd-plant-seed` wrapper expectations plus unrelated seed-audit and transition/uplift subprocess helpers.

## Reviewer Verdict

### Plan: Slice 8 ambiguity resolution

```text
VERDICT: PASS
REASONING: Alternative A is the single safest interpretation. The initiative exists to stop carrying stale overwrite snapshots where changes are anchor-targetable, and the Slice 7 design identifies `planning-config.md` as exactly that case: a stale overwrite with a small set of plausible modifier-owned deltas. Preserving current omissions byte-for-byte would convert upstream drift into local intent, while the seven-operation source-freshening candidate keeps within Phase 4's no-new-operation boundary and has already preflighted against real upstream content. The one caveat is that upstream's `gsd-sdk query` is the long-term supported programmatic surface, but this modifier carrier should keep the repo-local `gsd-tools.cjs` examples because the current modifier workflows still use that installed compatibility path.
RECOMMENDATION: Proceed with Alternative A. Let upstream-added field docs flow through; preserve the repo-local `gsd-tools.cjs` examples for this slice; preserve `workflow.discuss_mode: "exploratory"` and `/gsd-discuss-phase` naming; drop the current modifier `mode` row and use upstream's `interactive` / `yolo` row rather than encoding `code-first` / `plan-first` / `hybrid`.
EVIDENCE:
- .planning/initiatives/inject-migration/INITIATIVE.md:9 - initiative goal is surgical injection to reduce drift and eliminate silent staleness.
- .planning/initiatives/inject-migration/INITIATIVE.md:21 - the stated fix is to stop carrying snapshots where the change is anchor-targetable.
- .planning/initiatives/inject-migration/phases/04-first-wave-references.md:98 - Slice 8 is explicitly the apply migration slice for `references/planning-config.md`.
- .planning/initiatives/inject-migration/phases/04-first-wave-references.md:129 - Phase 4 is limited to reference migrations, not workflows or lib files.
- .planning/initiatives/inject-migration/phases/04-first-wave-references.md:131 - Phase 4 must not add new operation kinds.
- .planning/initiatives/inject-migration/decisions/MIG-design-planning-config.md:10 - design says not to treat this carrier as routine full-fidelity apply.
- .planning/initiatives/inject-migration/decisions/MIG-design-planning-config.md:14 - repo-local `gsd-tools.cjs` examples are identified as plausible modifier-owned content.
- .planning/initiatives/inject-migration/decisions/MIG-design-planning-config.md:16 - `workflow.discuss_mode: "exploratory"` is identified as plausible modifier-owned content.
- .planning/initiatives/inject-migration/decisions/MIG-design-planning-config.md:18 - dropped upstream field families are identified as likely upstream drift, not intentional modifier ownership.
- .planning/initiatives/inject-migration/decisions/MIG-design-planning-config.md:20 - design recommends source-freshening inject migration and letting upstream-added field docs flow through.
- .planning/initiatives/inject-migration/decisions/MIG-design-planning-config.md:410 - candidate preserves upstream-added field documentation.
- .planning/initiatives/inject-migration/decisions/MIG-design-planning-config.md:411 - candidate preserves upstream `mode` row instead of stale modifier `code-first` / `plan-first` / `hybrid`.
- .planning/initiatives/inject-migration/decisions/MIG-design-planning-config.md:424 - seven `block_replace` operations preflighted as applied.
- .planning/initiatives/inject-migration/decisions/MIG-design-planning-config.md:425 - preflight reports `verify_passed=True`.
- tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:466 - current manifest still uses `mode: "overwrite"` for Codex.
- tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:471 - current manifest still uses `mode: "overwrite"` for Claude.
- tooling/portable-gsd/overlay/get-shit-done/references/planning-config.md:56 - current modifier docs prefer `gsd-tools.cjs`.
- tooling/portable-gsd/overlay/get-shit-done/references/planning-config.md:228 - current modifier docs carry the stale `code-first` / `plan-first` / `hybrid` mode row.
- tooling/portable-gsd/overlay/get-shit-done/references/planning-config.md:255 - current modifier docs carry `workflow.discuss_mode` default `"exploratory"`.
- /home/rookslog/workspace/projects/get-shit-done-upstream/get-shit-done/references/planning-config.md:230 - upstream documents `mode` as `"interactive"` / `"yolo"`.
- /home/rookslog/workspace/projects/get-shit-done-upstream/get-shit-done/references/planning-config.md:252 - upstream includes newer `workflow.auto_prune_state`.
- /home/rookslog/workspace/projects/get-shit-done-upstream/get-shit-done/references/planning-config.md:265 - upstream includes `workflow.inline_plan_threshold`.
- /home/rookslog/workspace/projects/get-shit-done-upstream/get-shit-done/references/planning-config.md:280 - upstream includes `ship.pr_body_sections`.
- /home/rookslog/workspace/projects/get-shit-done-upstream/get-shit-done/references/planning-config.md:312 - upstream includes `features.global_learnings`.
- /home/rookslog/workspace/projects/get-shit-done-upstream/get-shit-done/references/planning-config.md:328 - upstream includes `learnings.max_inject`.
- /home/rookslog/workspace/projects/get-shit-done-upstream/get-shit-done/references/planning-config.md:336 - upstream includes `intel.enabled`.
- tooling/portable-gsd/overlay/get-shit-done/templates/config.json:2 - local template uses `"mode": "interactive"`.
- .planning/config.json:46 - this repo's active config uses `"mode": "yolo"`.
- tooling/portable-gsd/overlay/get-shit-done/workflows/discuss-phase.md:194 - modifier workflow reads `workflow.discuss_mode` via repo-local `gsd-tools.cjs`.
- tooling/portable-gsd/overlay/get-shit-done/workflows/discuss-phase.md:197 - modifier workflow validates `exploratory`, `discuss`, and `assumptions` as discuss modes.
- .codex/get-shit-done/bin/gsd-tools.cjs:4 - local `gsd-tools.cjs` is deprecated in favor of `gsd-sdk query`.
- .codex/get-shit-done/bin/gsd-tools.cjs:5 - same file remains the compatibility implementation for shell scripts and older workflows.
```

Action taken: proceeded with Alternative A. No contract code changed.

### gsd-debugger: broad unittest failure disposition

```text
VERDICT: PASS
REASONING: The failed broad unittest gate is not flaky and is not caused by the Slice 8 `planning-config.md` migration; it matches the already-recorded OOS #5 broad-discover baseline. The failing assertions still inspect `gsd-from-gsd2` and `gsd-plant-seed` as old `overwrite` carriers under `tooling/portable-gsd/overlay/skills/...`, but those carriers were intentionally reclassified in Phase 0 to `mode: add` under `harness_modifier/overlay/skills/...`. The current slice diff is confined to the `planning-config.md` manifest migration, deletion of the old overwrite carrier, and seven new `planning-config` inject source files, while focused manifest/inject/apply verification has already passed; therefore the correct disposition is to treat full-discover as the known non-carrier baseline and avoid unrelated test/helper edits.
RECOMMENDATION: Proceed with Slice 8 on the focused carrier gates already passed; record the full-discover failure as the known OOS #5 baseline in the slice checkpoint/STATE, and do not edit stale `gsd-from-gsd2`/`gsd-plant-seed`, seed-audit, or transition/uplift tests in this slice.
EVIDENCE:
- `.planning/initiatives/inject-migration/STATE.md:81` records OOS #5 as stale `gsd-from-gsd2`/`gsd-plant-seed` source-inspection tests plus seed-audit and transition/uplift helper failures.
- `.planning/initiatives/inject-migration/STATE.md:168` and `.planning/initiatives/inject-migration/STATE.md:170` record prior Phase 4 Slice 2/4 broad-discover failures as the same known baseline outside carrier write sets.
- `.planning/initiatives/inject-migration/STATE.md:173` records Phase 4 Slice 6 debugger PASS for this same broad full-discover baseline with no write-set expansion.
- `.planning/initiatives/inject-migration/phases/04-first-wave-references.md:98` defines Slice 8 as applying migration of `references/planning-config.md`.
- `tooling/codex/tests/test_health_and_migration_follow_through_contract.py:14` expects `skills/gsd-from-gsd2/SKILL.md` to be `overwrite`, and `:36` reads the old `tooling/portable-gsd/overlay/skills/...` path.
- `tooling/codex/tests/test_seed_consumer_follow_through_contract.py:13` expects `skills/gsd-plant-seed/SKILL.md` to be `overwrite`, and `:38` reads the old `tooling/portable-gsd/overlay/skills/...` path.
- `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:1203` and `:1205` show `gsd-from-gsd2` is actually `mode: add` from `harness_modifier/overlay/skills/...`; `:1236` and `:1238` show the same for `gsd-plant-seed`.
- `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:461` through `:523` show the Slice 8 carrier is `get-shit-done/references/planning-config.md` with `mode: inject`.
- `git status --short` shows only `OVERLAY-MANIFEST.json`, deleted `tooling/portable-gsd/overlay/get-shit-done/references/planning-config.md`, untracked `.planning/debug/`, and untracked `harness_modifier/overlay/inject-sources/get-shit-done/references/planning-config/`; no failing test/helper surface is part of the slice diff.
```

Action taken: proceeded on focused gates; no write-set expansion.

## Auto-Recovery

- Broad full discover failed, was retried once, reproduced the same 452-test / 2-failure / 4-error baseline, and was sent to `gsd-debugger`.
- `gsd-debugger` returned PASS that the failure is unrelated to this carrier and no fix exists inside the Slice 8 write set.

## Observations

- The source-freshening behavior is intentional and reviewer-approved. Materialized output keeps upstream-added fields such as `workflow.security_*`, `ship.pr_body_sections`, `features.global_learnings`, `learnings.max_inject`, and `intel.enabled`.
- The materialized `mode` row follows upstream (`interactive` / `yolo`) instead of preserving the stale current overwrite row (`code-first` / `plan-first` / `hybrid`).
- The materialized command examples keep repo-local `gsd-tools.cjs` because the current modifier workflows still use that installed compatibility path.
- The `gsd-debugger` reviewer wrote an untracked `.planning/debug/phase-04-slice-08-gate-failure.md` artifact despite the read-only reviewer contract; the main loop removed it before commit and did not stage it.

## Next expected slice

- 04-first-wave-references slice 09 - phase debrief and migration metrics.
