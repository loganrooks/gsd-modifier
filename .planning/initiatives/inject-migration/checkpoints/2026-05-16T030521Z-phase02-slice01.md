# Checkpoint phase 02 slice 01

Timestamp: 2026-05-16T03:05:21Z
Phase: 02-contract-tools
Slice: 01-schema-reader
Outcome: success
Commit: (set post-commit)

## What was done

Implemented the schema reader for `OVERLAY-MANIFEST.json` `schema_version: 4` and `mode: inject`. Parser-only per the slice boundary; apply-time and verify-time inject logic are reserved for Slices 2-4.

Artifacts:

1. **NEW** `harness_modifier/contract/inject_operations.py` — new module that codifies ADR-001 §3 catalog (7 operation kinds), §3 universal `marker_key` field (with regex per §4 convention), §5 `parity_intent` vocabulary (outcome_aligned | runtime_independent). Per-kind validators return `list[str]` of error messages; `validate_inject_materializer` returns `(errors, marker_keys)` so the caller can detect cross-entry and intra-runtime duplicates.
2. **EDIT** `harness_modifier/contract/portable_gsd_contract.py` (+144 / -21) —
   - `VALID_MODES` expanded with `"inject"`; new `SUPPORTED_SCHEMA_VERSIONS = {2, 3, 4}` constant
   - `load_overlay_manifest_specs` dispatches schema 4; for inject mode delegates to the new `normalize_overlay_inject_materializer_entry` which sets `mode: inject`, `operations: [...]`, empty `source_path` / `source_rel_path`
   - v3 entry-shape validation block widened to `(3, 4)` (same parity_tier / materializers shape per ADR §6)
   - New schema-4-specific block validates: `parity_intent` required for any entry that declares a `mode: inject` materializer; per-operation validation via `inject_operations.validate_inject_materializer`; intra-runtime duplicate marker_key detection (same key twice in one materializer's operations list); cross-entry collision detection (same key in two different entries)
   - The marker_key uniqueness rule preserves ADR §5 outcome_aligned mirroring: the SAME key may appear in the codex AND claude materializers WITHIN one entry (intentional). Across entries: hard fail.
   - `missing_source_files` and `external_source_entries` filter `mode != "inject"` so inject entries do not spuriously show up
   - `apply_overlay` skips inject entries (TODO marked at Slice 2)
   - `build_materialization_report_for_roots` skips inject entries in the content-mismatch path (TODO marked at Slice 4); `missing_live_targets` still applies (the inject target IS the upstream-installed file, so its presence is the minimum assertion the Slice 1 parser-only boundary can support)
3. **NEW** `tooling/codex/tests/test_inject_schema.py` — 23 tests:
   - `V4SchemaParserTests` (13): happy path, missing parity_intent, invalid parity_intent value, unknown operation kind, missing required field on operation, missing marker_key, invalid marker_key format, cross-entry duplicate marker_key, intra-runtime duplicate marker_key, outcome_aligned mirroring (negative case for cross-runtime sharing within one entry), v3 manifest rejects mode:inject, mixed-mode (overwrite + add + inject) coexistence, validate-manifest accepts v4 manifest end-to-end
   - `PerOperationKindValidatorTests` (10): one happy-path per operation kind (7) + marker_key pattern accepts ADR-documented examples + marker_key pattern rejects malformed inputs + OPERATION_KINDS frozenset matches the ADR §3 catalog

## Files touched

- `harness_modifier/contract/portable_gsd_contract.py` (modified)
- `harness_modifier/contract/inject_operations.py` (added)
- `tooling/codex/tests/test_inject_schema.py` (added)
- `.planning/initiatives/inject-migration/STATE.md` (modified — state mechanics)
- `.planning/initiatives/inject-migration/checkpoints/2026-05-16T030521Z-phase02-slice01.md` (added — this file)

## Verification gates run

- `git diff --check` → exit 0
- `python3 -m py_compile harness_modifier/contract/{portable_gsd_contract,inject_operations,__init__}.py` → exit 0
- `python3 -m unittest tooling.codex.tests.test_inject_schema -v` → 23/23 OK
- `python3 -m unittest tooling.codex.tests.test_portable_gsd_contract` → 10/10 OK (no v3 regression)
- `python3 tooling/codex/audit_refmap.py verify .` → exit 0
- `python3 tooling/codex/scan_threshold_language.py --ignore-meta-instruction-lines harness_modifier/contract/inject_operations.py harness_modifier/contract/portable_gsd_contract.py tooling/codex/tests/test_inject_schema.py` → exit 0 (no findings)

(Per OOS #3 / Phase 2 operator direction, `bash scripts/ci/check-bootstrap.sh` is excluded from slice gates. `bash scripts/ci/check-deterministic.sh` is a phase-boundary gate, not per-slice.)

## Reviewer Verdict

`adversarial-auditor-xhigh` (pre-commit, contract surface change per PROTOCOL.md "Per-slice mandate" → "Phase 2 contract code slices: pre-commit reviewer on the proposed diff"):

```
VERDICT: PASS
REASONING: Reviewed the new module harness_modifier/contract/inject_operations.py (~180 lines), the diff to harness_modifier/contract/portable_gsd_contract.py (+144/-21), and the 23-test smoke suite tooling/codex/tests/test_inject_schema.py against the binding ADR-001 (especially the §3 7-kind catalog, §3 universal marker_key paragraph from commit 49aee86, §4 marker convention, §5 parity_intent semantics, §6 backward compatibility) and Phase 2 Slice 1 boundary "parser only; no apply-time or verify-time logic." The parser faithfully implements all seven operation kinds with their per-kind required fields; marker_key is enforced universally with a regex matching the §4 convention; parity_intent is required for inject entries and validated against the §5 vocabulary; the schema_version dispatch correctly admits 2/3/4, rejects mode:inject under schema_version <3 with a clear ValueError ("requires schema_version >= 4"), and widens the existing v3 parity_tier/materializers checks to apply equally to v4 (correct per ADR §6's "all v3 modes ... PLUS the new inject mode"). The marker_key uniqueness reading — "same key + same entry + different runtimes = allowed (outcome_aligned mirroring); same key + different entries = hard fail; same key + same runtime + multiple ops = hard fail" — is the correct interpretation of §3's "globally unique across the manifest (across all entries and all operations)" reconciled with §5's outcome_aligned semantics where codex and claude materializers within one entry intentionally mirror the same operation/key. The apply/verify defensive skips are appropriately bounded: apply_overlay skips inject entries with a TODO comment pointing at Slice 2, and build_materialization_report_for_roots skips inject in the content_mismatch path with a TODO pointing at Slice 4; neither smuggles in semantic decisions — they preserve "target exists" as the only assertion, which is the minimum the parser-only boundary can support. The 23 tests cover happy path + each load-bearing failure case, the v3-rejects-inject backward-compat path, and a mixed-mode v4 manifest exercising overwrite + add + inject coexistence. The 23 new tests pass; pre-existing failures in unrelated test files were confirmed baseline (not introduced by this diff).
RECOMMENDATION: proceed. Optional polish (any of these can be deferred to Slice 2-4 without risk):
  (a) silent skip in apply_overlay/build_materialization_report could log a debug-level note for operator clarity if a v4 manifest is materialized mid-migration — but for the parser-only boundary, silent skip is defensible because no v4 entry exists in OVERLAY-MANIFEST.json until Phase 3;
  (b) Slice 2's apply_inject_operations integration should guard `spec.get("operations", [])` or branch on mode first (the inject spec dict carries `operations` only when mode=="inject");
  (c) Slice 3+ could optionally include the marker_key value in cross-entry collision messages (currently lists locations as "entry_id (runtime#index)" — already good).
EVIDENCE: ADR §3 universal marker_key at decisions/ADR-001:56 implemented at inject_operations.py:38-40 + enforced at validate_operation:131-133; ADR §3 7-kind catalog at ADR-001:54-123 matches OPERATION_KINDS frozenset at inject_operations.py:24-34; ADR §4 marker pattern at ADR-001:144-150 admits documented examples verified by test_marker_key_pattern_accepts_documented_examples; ADR §5 parity_intent values at ADR-001:166-182 implemented at inject_operations.py:36 + enforced at portable_gsd_contract.py inject validation block; ADR §6 backward compat preserved (v3 rejection of mode:inject with clear ValueError; mixed-mode v4 verified by test_v4_manifest_with_mixed_modes); Slice 1 boundary "parsing only" honored (apply skip + verify skip with explicit Slice 2/4 TODO citations); marker_key uniqueness reading reconciles ADR §3 globally-unique with §5 outcome_aligned correctly per test_v4_manifest_allows_shared_marker_key_across_runtimes_within_entry; scope discipline: write set matches Slice 1 declaration exactly.
```

**Action taken**: proceeded to commit per PASS verdict. Three polish notes captured here for Slice 2-4 implementers; none blocking Slice 1 commit.

## Auto-Recovery

none — all gates passed on first execution; reviewer returned PASS on first call.

## Observations

- **Boundary discipline preserved**: the apply_overlay and build_materialization_report skips name the future slice (Slice 2 / Slice 4) in their TODO comments. This is in-code documentation that the parser-only boundary is intentional and tracked.
- **marker_key uniqueness rule** required careful reconciliation between ADR §3 ("globally unique") and ADR §5 (outcome_aligned mirroring). Reviewer confirmed the chosen reading. Phase 3+ migrations should re-test against this once a real v4 entry lands.
- **No `__init__.py` edit needed**: the slice spec said "Possibly EDIT" because import via `from harness_modifier.contract import inject_operations` does not require re-export. Both portable_gsd_contract.py and the test suite import this way successfully.
- **Pre-existing unrelated test failures** in the full unittest suite (not in scope for this slice's gates) are baseline state; Slice 5's "full discover" gate will need to reconcile or document them at phase boundary.

## Next expected slice

- **Phase 2 Slice 2**: implement `apply_inject_operations(content: str, operations: list[dict], source_resolver: callable) -> tuple[str, list[OperationRecord]]` in `inject_operations.py`. Per-kind apply-time semantics; in-memory pure function; pre-flight atomicity (all operations computed before any write); idempotency (re-applying produces identical content). Reviewer note (b) above will be relevant when integrating with `portable_gsd_contract.py`'s materialization codepath.
