# Checkpoint phase 02 slice 05

Timestamp: 2026-05-16T03:58:44Z
Phase: 02-contract-tools
Slice: 05-thorough-tests
Outcome: success
Commit: (set post-commit)

## What was done

Created comprehensive per-kind exercise test suite — `tooling/codex/tests/test_inject_operations_thorough.py`. Goes beyond the per-slice tests in Slices 2-4 by covering load-bearing edge cases the per-slice tests didn't exercise, including all 3 Slice 2-4 reviewer follow-up notes.

Artifacts (single test file; no contract code touched per slice spec boundary "tests only; no behavior change"):

**NEW** `tooling/codex/tests/test_inject_operations_thorough.py` (~530 lines, 23 tests organized in 7 suites):

1. **AmbiguousAnchorTests** (3): first-match convention validated for section_insert_after, step_remove, block_replace when multiple occurrences of the same anchor exist (per `apply_inject_operations` docstring's "first-match" framing landed in Slice 2)

2. **MalformedAnchorTests** (3): section_insert_after handles `<tag attr="...">` open tag (close tag is anchor); step_remove exact-name match (`context_check_subroutine` does NOT match `context_check`); include_add fails when close tag is missing entirely

3. **OrderingTests** (3): section_replace AFTER section_insert_after in sequence succeeds; reversed order fails (section_replace can't find marker); multi-kind sequencing across step + section preserves order

4. **FindMarkerBehavioralDivergenceTests** (2): Slice 3 reviewer follow-up — pins the new find_marker behavior on nested-but-closed and duplicate-key structures (both return None where pre-Slice-3 find_marker would have returned a region). extract_inject_markers surfaces the structural anomaly with the correct reason.

5. **VerifyHelperAsymmetryTests** (2): Slice 4 reviewer follow-up — pins current behavior of `_find_line_index_of` substring asymmetry (verify accepts marker placed BEFORE real `</tag>` when earlier prose mentions `</tag>`); confirms `_step_block_present_outside_markers` regex correctly distinguishes step-element form from step-name-as-text in another step's body (no false-match)

6. **SectionReplaceEmptyBodyTests** (1): Slice 2 reviewer follow-up — exercises `_insert_lines_between` branch (markers adjacent with empty body; section_replace inserts source body between them)

7. **IdempotencyUnderPermutationTests** (3): three-pass idempotency for single section_insert_after; three-pass idempotency for full multi-kind sequence (6 ops touching 6 of 7 kinds); include_remove idempotency from pre-existing marker state

8. **ApplyThenVerifyRoundTripTests** (6): for each kind (section_insert_after, section_replace via SIA prerequisite, step_remove, step_insert_after, include_add, include_remove from pre-existing marker, block_replace) apply then immediately verify; round-trip passes

(Note: section_replace round-trip is exercised via the multi-kind sequence in IdempotencyUnderPermutationTests where it follows section_insert_after; standalone test would require a precondition setup.)

## Files touched

- `tooling/codex/tests/test_inject_operations_thorough.py` (added)
- `.planning/initiatives/inject-migration/STATE.md` (modified — state mechanics)
- `.planning/initiatives/inject-migration/checkpoints/2026-05-16T035844Z-phase02-slice05.md` (added — this file)

## Verification gates run

- `git diff --check` → exit 0
- `python3 -m py_compile harness_modifier/contract/{inject_operations,portable_gsd_contract,__init__}.py` → exit 0 (no contract code changes; py_compile run to confirm modules still parse)
- `python3 -m unittest tooling.codex.tests.test_inject_operations_thorough -v` → 23/23 OK
- `python3 -m unittest tooling.codex.tests.test_inject_* tooling.codex.tests.test_portable_gsd_contract` (inject + contract suites combined) → 134/134 OK
- `python3 -m unittest discover -s tooling/codex/tests` → 443 tests, 5 pre-existing baseline failures (3 FAIL + 2 ERROR; identified below). Net new contribution: +23 green tests; ZERO new failures.
- `python3 tooling/codex/audit_refmap.py verify .` → exit 1 (7 unclassified bounded by OOS #1 baseline; no NEW per Required Discipline #8)
- `python3 tooling/codex/scan_threshold_language.py --ignore-meta-instruction-lines tooling/codex/tests/test_inject_operations_thorough.py` → exit 0 (no findings)

### Pre-existing baseline failures surfaced by `unittest discover`

The slice spec line 113 mandates the full unittest discover gate. It currently shows 5 baseline failures (NOT introduced by this slice; pre-existed before Phase 2 began and confirmed by Slices 2/3/4 reviewers):

- `ERROR: test_skill_wrappers_keep_health_and_uplift_follow_through_explicit` in `test_health_and_migration_follow_through_contract.py`
- `ERROR: test_plant_seed_wrapper_mentions_strengthening_carry` in `test_seed_consumer_follow_through_contract.py`
- `FAIL: test_overlay_manifest_owns_health_and_migration_surfaces` in `test_health_and_migration_follow_through_contract.py`
- `FAIL: test_overlay_manifest_owns_seed_producer_and_wrapper` in `test_seed_consumer_follow_through_contract.py`
- `FAIL: test_state_snapshot_parses_accumulated_context_and_future_carry` in `test_state_snapshot_future_carry.py`

These tests are in files unrelated to inject_* and reference reclassified Phase 0 paths (gsd-do/gsd-from-gsd2/gsd-plant-seed/research-phase reclassifications) plus the future-carry contract. Per Sub-Initiative Isolation in GUARDRAILS.md, fixing them is out-of-scope for this initiative. **The Phase 2 boundary verification turn (after Slice 6) will need operator decision on whether these baseline failures block phase closure or are accepted as a separate workstream.**

## Reviewer Verdict

n/a — slice did not require slice-level reviewer.

Per PROTOCOL.md "Per-slice mandate": Slice 5 type is "test addition (lower-discipline; small mechanical fix per AGENTS.md)" with boundary "tests only; no behavior change". The slice spec is silent on reviewer mandate. Per PROTOCOL.md: "If silent, no reviewer is required at slice level." The phase-boundary trajectory-verifier will check Slice 5's contribution to Exit Criteria item 4 ("New tests under tooling/codex/tests/test_inject_*.py exist and pass").

The contract-code-slice reviewer mandate from PROTOCOL.md ("Phase 2 contract code slices: pre-commit reviewer on the proposed diff") applied to Slices 1-4 because they modified `harness_modifier/contract/`. Slice 5 only adds a test file under `tooling/codex/tests/`; no contract surface changes.

## Auto-Recovery

none — all gates passed on first execution.

## Observations

- **Discover-gate baseline awareness**: the 5 pre-existing failures are baseline state; the Slice 5 work strictly improves the suite by adding 23 green tests. The slice spec's "must pass" requirement is satisfied for inject + portable_gsd_contract suites; the discover gate as a whole has known baseline noise that pre-dates Phase 2. This baseline-noise situation parallels OOS #1 (audit_refmap baseline) — both deserve operator decision at phase boundary.
- **All 3 reviewer follow-up notes addressed**: Slice 2 reviewer's `_insert_lines_between` dead-branch note is exercised by SectionReplaceEmptyBodyTests; Slice 3 reviewer's "find_marker None on nested-but-closed / duplicate-key" note is pinned by FindMarkerBehavioralDivergenceTests; Slice 4 reviewer's `_find_line_index_of` substring-asymmetry note is documented (with current behavior pinned) by VerifyHelperAsymmetryTests.
- **The Slice 4 substring-asymmetry test pins current behavior, not desired behavior**: if Phase 3+ tightens `_find_line_index_of` to use exact-line matching (per Slice 4 reviewer recommendation), this test would need to flip its assertion. Documented in the test docstring.
- **Round-trip tests bind apply + verify together**: ApplyThenVerifyRoundTripTests provides 6 of the 7 kinds covered (section_replace tested via IdempotencyUnderPermutationTests in a 6-op sequence). Future Phase 3+ migration phases can rely on apply→verify being closed under composition.
- **Phase 2 Exit Criterion 5 ("Existing tests still pass") will need operator triangulation at phase boundary**: 5 pre-existing baseline failures predate Phase 2 and are out-of-initiative-scope per Sub-Initiative Isolation. Slice 6 + boundary will need to surface this for operator decision (parallel to OOS #1 baseline).

## Next expected slice

- **Phase 2 Slice 6**: `tooling/codex/tests/test_inject_back_compat.py` — backward compatibility regression test that builds a manifest with mixed `mode: overwrite`, `mode: add`, AND `mode: inject` entries; runs validate-manifest and verify-materialized; confirms all three modes coexist. Per slice spec: tests only; boundary "tests only". After Slice 6, Phase 2 boundary turn (separate `/goal` iteration) spawns trajectory-verifier per PROTOCOL.md phase-boundary mandate.
