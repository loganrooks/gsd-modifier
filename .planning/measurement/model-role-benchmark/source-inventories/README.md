# Source Inventories

This directory stores compact, commit-visible opinion-mining outputs.

Expected run shape:

- `<timestamp>/metadata.json`
- `<timestamp>/inventory.jsonl`
- optional reviewer notes or synthesis inputs

Raw HTML and full extracted text are intentionally stored outside this directory in ignored cache paths.
