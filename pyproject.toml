[project]
name = "gsd-modifier"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "PyYAML>=6.0.2,<7",
    "requests>=2.32.0,<3",
]

[tool.uv]
package = false
